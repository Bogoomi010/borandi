Original prompt: 완성된 에셋으로 게임 스테이지를 최대 15스테이지까지 구성한다. 각기 다른 맵의 형태를 지닌다. 필요한 에셋이 있으면 추가 생성하여 사용한다. 완성되고 정상적으로 게임 플레이가 되는것까지 확인 후 종료한다.

## Progress

- Added the dark fantasy village tileset asset sheet in the previous commit.
- Current task: wire that completed asset into playable game stages, up to 15 distinct stage maps, then verify gameplay.

## Notes

- Project uses Vite + TypeScript; per AGENTS instructions use `yarn` for build/test commands.
- Added `src/data/stages.ts` with 15 distinct stage maps, names, ground themes, paths, and decorations.
- Reworked path helpers and board rendering to use per-stage paths and draw decorations from the generated dark fantasy tileset PNG.
- Corrected stage semantics after user clarification: the player now selects one map at run start, plays a 40-round arc on that fixed map, and unlocks the next map only after clearing the round 40 boss.
- Hardened the profile unlock rule so even a bad caller cannot unlock the next map before a cleared run reaches the 40-round final boss condition; added focused tests for pre-40 clear flags, 40R clears, and 40R losses.
- Restored wave progression to 40 rounds with bosses at 10/20/30/40; boss rounds now require the boss kill before round completion.
- Added `stageId` to game state, save metadata, replay/checksum, result summaries, and render_game_to_text.
- Added map selection and lock display to the new game modal; profile stores `unlockedStage`.
- Added five difficulties for balance testing: beginner, normal, intermediate, expert, and master.
- Added simulation controls for max grade and max legend count so balance checks can verify "no legend", "2 legends", and "5 legends" constraints.
- Tuned difficulty HP/economy/unit-cap/enemy-limit values and late-grade unit stats using automated play matrices:
  - Beginner clears with hero-and-below only: 30/30 clears.
  - Normal needs legend help: 0 legends 2/30 clears, 2 legends 10/30 clears.
  - Intermediate rejects low legend counts but clears with enough scaling: 2 legends 4/30 clears, 5 legends 15/30 clears, unrestricted average 14.7 legends 30/30 clears.
  - Expert needs more than the intermediate legend budget: 5 legends 0/30 clears, unrestricted average 13.8 legends 19/30 clears.
  - Master is intentionally severe in balanced autoplay: unrestricted 0/30 clears.
- Improved the autoplay balance harness so legendary crafting and legend-limit pruning prefer a useful spread of legend units instead of stacking/saving only one high boss-damage recipe.
- Added `yarn balance` as a repeatable 30-seed five-difficulty balance gate matching the current target bands.
- Added `yarn balance --json=...` for storing scenario reports and pass/fail gate results as machine-readable evidence.
- Added an in-game `Tools > 5난이도 밸런스 게이트` modal that reuses the same gate logic and can export Markdown/JSON reports.
- Added deterministic testing hooks: `window.render_game_to_text`, `window.advanceTime(ms)`, and DEV-only `window.__randi_dev` for browser playtests.
- Added `difficulty` and `round` to `render_game_to_text` so browser playtests can verify the selected difficulty and current run state directly.
- Re-ran the 30-seed five-difficulty balance gate on the current tree; all gates passed with 입문자 100.0%, 일반 0전설 6.7%, 일반 2전설 33.3%, 중급자 2전설 13.3%, 중급자 5전설 50.0%, 중급자 제한 없음 100.0%, 고수 5전설 0.0%, 고수 제한 없음 63.3%, 초고수 제한 없음 0.0%.
- Verified the browser new-game UI starts all five difficulties and that `render_game_to_text` reports the expected difficulty id/name, stage, round, gold, life, and enemy limit.
- Extended DEV-only `window.__randi_dev.newRun` to support `newRun(seed, difficulty, stageId)` while preserving the old `newRun(seed, stageId)` shape, so browser playtests can jump directly into any of the five difficulties.
- Ran a short browser direct-play loop on all five difficulties using summon/startWave/advanceTime. With the same early actions, 입문자 reached 3R with pressure 0/100, 일반 41/54, 중급자 34/52, 고수 41/46, and 초고수 ended at 3R with pressure 39/32. This supports the current early-game difficulty separation but is still not a substitute for the requested 2-hour manual pass.
- Added `unitSummary` and `boss` to `render_game_to_text`, including grade counts, legend-or-better count, max grade, boss kills, and boss failed rounds, so browser playtests can inspect whether runs match the legend-count balance targets.
- Verified the new browser state fields with targeted DEV-spawn checks: normal with 2 legends reported `legendOrBetter: 2`, intermediate with 5 legends reported `legendOrBetter: 5`, and master with no legends reported `legendOrBetter: 0` while ending early at 40/32 pressure.
- Added `yarn browser-balance` as a Playwright-backed browser runtime playtest harness. With a running `yarn dev` server, it starts target difficulty scenarios through DEV hooks, advances real browser game time, records JSON, and can capture screenshots.
- Latest browser-balance smoke reached 11R after the 10R boss with 입문자/전설 없음, 일반/전설 2개, 중급자/전설 5개, and 고수/전설 5개. Boss kill times separated difficulty feel: 입문자 8.5s, 일반 131.8s, 중급자 16.2s, 고수 66.4s. 초고수/전설 없음 ended at 3R with 39/32 pressure.
- Upgraded `yarn browser-balance` from a report-only smoke into a pass/fail browser gate. It now exits nonzero if the browser runtime no longer proves the five early-play criteria, and writes `passed` plus per-gate details into JSON.
- Latest browser-balance gate passed all 5 checks: 입문자 no-legend reached 11R after 10R boss, normal 2-legend reached 11R, intermediate 5-legend reached 11R, expert 5-legend had a first-boss kill at least 2x slower than intermediate, and master no-legend collapsed by 3R at 39/32 pressure.
- Added browser-balance negative-control scenarios for normal 2-hero and intermediate 2-legend. Latest 7-gate run passed: normal 2-hero collapsed at 7R with 68/54 pressure while normal 2-legend reached 11R; intermediate 2-legend reached 11R but killed the 10R boss in 134.5s versus 16.2s for 5 legends.
- Added `yarn browser-direct`, a Playwright-backed browser sampler that does not DEV-spawn fixed units. It starts target difficulty runs through the DEV new-run hook, then repeatedly uses the real action path for summon, selector picks, 3-merge, craft, upgrade, startWave, and advanceTime to gather longer direct-play balance evidence.
- Added DEV-only `window.__randi_dev.balanceSnapshot()` so browser balance tooling can read craftable recipes, selector candidates, upgrade costs, unit grades, and unit scores without mutating game state.
- Latest `yarn browser-direct --seeds=1` observation: 입문자/무전설 cleared 40R with 0/100 pressure, 일반/0전설 failed at 40R with 60/54 pressure, 일반/2전설 failed at 39R with 77/54 pressure, 중급자/2전설 failed at 37R, 중급자/5전설 cleared 40R, 고수/5전설 failed at 40R, 고수/제한 없음 failed at 40R with lower pressure, and 초고수/제한 없음 failed at 32R. This is useful browser-runtime direct-play evidence, but the normal-difficulty single seed is noisy and still needs broader manual/playtest coverage.
- Latest `yarn balance --json=output/current-balance.json` 30-seed gate passed all 5 target bands: 입문자/전설 없음 100.0%, 일반 0전설 6.7% vs 2전설 33.3%, 중급자 2전설 13.3% vs 5전설 50.0% and unrestricted 100.0%, 고수 5전설 0.0% vs unrestricted 63.3%, 초고수 unrestricted 0.0%.
- `yarn browser-direct` now records per-run and total simulated play seconds in JSON. Latest `yarn browser-direct --seeds=2` covered 16 browser-runtime direct-play runs and 555.9 minutes / 9.27 hours of simulated game time: 입문자/무전설 2/2 clears, 일반/0전설 0/2 clears, 일반/2전설 0/2 clears but higher average round and lower pressure than 0전설, 중급자/2전설 1/2 clears, 중급자/5전설 1/2 clears, 고수/5전설 0/2 clears, 고수/unrestricted 1/2 clears, 초고수/unrestricted 0/2 clears. The intermediate two-seed direct sampler remains noisy; rely on `yarn balance` for the 30-seed target-band gate.
- Tightened the intermediate balance gate to better match "전설 5개 이상": 중급자/2전설 must now stay at or below 15% clear rate, and 중급자/5전설 must clear at least 30%p more often than 2전설. Latest `yarn balance --json=output/current-balance.json` still passed with 중급자/2전설 13.3%, 중급자/5전설 50.0%, and 중급자/unrestricted 100.0%.
- Added `yarn balance-audit`, which reads the `yarn balance`, `yarn browser-balance`, and `yarn browser-direct` JSON outputs and prints a single requirement-by-requirement Markdown audit. It explicitly marks the requested real 2-hour manual play session as MISSING so automated evidence is not mistaken for full completion.
- Extended `yarn balance-audit` with `--manual=...` support and added `docs/manual-balance-playlog.example.json`; the manual item passes only when logged sessions cover all five difficulties and total at least 120 minutes.
- Latest `yarn balance-audit --out=output/balance-audit.md` loaded all three JSON sources and reported PASS for five difficulties, novice no-legend, normal 0-vs-2 legend, intermediate 2-vs-5 legend, expert 5-legend-vs-open, master difficulty, browser 10R gates, and browser direct automated samples. It reported MISSING only for the real 2-hour human manual play session.
- Latest checks passed: `yarn test`, `yarn build`, `yarn check`, `yarn balance`, `yarn browser-balance`, `yarn browser-direct --seeds=1`, `yarn browser-direct --seeds=2`, `yarn balance-audit`, and the web game Playwright smoke client against the local dev server.
- Tightened map progression again after clarification: a 40R clear only unlocks the next map when the cleared map is exactly the current unlocked frontier, so bypassed/locked map clears cannot skip the intended map-by-map progression.
- Tightened the normal-difficulty balance target to include a 1-legend scenario, then softened only normal difficulty slightly. Latest `yarn balance --json=output/current-balance.json` passed with 입문자/전설 없음 100.0%, 일반 0전설 20.0% vs 1전설 40.0% vs 2전설 56.7%, 중급자 2전설 13.3% vs 5전설 50.0% and unrestricted 100.0%, 고수 5전설 0.0% vs unrestricted 63.3%, 초고수 unrestricted 0.0%.
- Softened expert slightly after browser-direct showed unrestricted expert was too brittle under real input automation; the 30-seed gate still passed with 고수 5전설 0.0% vs unrestricted 63.3%, and targeted browser-direct expert seeds passed with 5전설 0/2 clears vs unrestricted 1/2 clears.
- Browser-direct now reuses the same seed index across compared scenarios, so 0/1/2 legend and 2/5 legend comparisons are less polluted by unrelated RNG differences. Targeted browser-direct checks passed for normal 0/1/2 legend, intermediate 2/5 legend, and expert 5-legend/open comparisons, but the full 2-seed strict browser-direct sweep remains noisy and should be treated as auxiliary evidence rather than the primary gate.
- Added `yarn manual-playlog` so real human play sessions can be appended to `output/manual-balance-playlog.json` with difficulty, time, result, stage, round, legend count, and notes; `yarn balance-audit --manual=...` can then evaluate the requested 120-minute / five-difficulty manual evidence.
- Result summaries now include difficulty id, legend/hidden counts, wall-clock session duration, and a ready-to-run `yarn manual-playlog` command in the result modal/report, so a human playtest can be logged immediately after each run.
- `yarn balance-audit --manual=...` now checks manual logs for the actual target outcomes, not just time: novice no-legend clear, normal 1-2 legend clear, intermediate 5+ legend clear, expert 5-or-fewer legend loss plus 6+ legend clear, and master loss.
- Manual target auditing now requires 40R evidence for manual clear rows and for the expert 5-or-fewer legend failure case, so an early or mislabeled clear cannot satisfy the requested 40-round balance proof.
- `yarn balance-audit` now audits browser-direct JSON by target scenario and per-difficulty observation, instead of accepting any nonzero simulated browser-direct time as sufficient automated direct-input evidence.
- Browser-direct selection now receives unit roles from the DEV balance snapshot and scores legend choices with family/role diversity bonuses, so 5-legend samples are less likely to waste the higher legend budget on redundant roles.
- Latest browser-direct strict pass after role-diverse legend selection: 입문자/무전설 cleared 40R, 일반/0전설 failed 38R while 1전설 and 2전설 cleared 40R, 중급자/2전설 failed 40R while 5전설 cleared 40R, 고수/5전설 failed 40R while unrestricted cleared 40R with 14 legends, and 초고수/unrestricted failed at 3R.
- Latest 30-seed `yarn balance` gate still passed after the browser-direct tooling change: 입문자 100.0%, 일반 0/1/2전설 20.0%/40.0%/56.7%, 중급자 2전설 13.3% vs 5전설 50.0%, 고수 5전설 0.0% vs unrestricted 63.3%, 초고수 0.0%.
- `yarn balance-audit` now also requires the browser-direct payload's own `passed: true` and at least five passing observation gates, so a strict-failed direct-input report cannot be accepted by fallback row calculations.
- Browser-direct intermediate comparison now treats lower pressure as improvement when both 2-legend and 5-legend samples fail on the same round, matching the normal/expert direct-input comparison semantics.
- Browser-direct now performs legal hero-to-legend 3-merges when the scenario permits more legends, making direct-input samples closer to real late-game play instead of leaving many heroes unmerged.
- Browser-direct now defaults to 2 seeds, and `yarn balance-audit` requires at least 2 direct-input seeds before accepting browser-direct as sufficient automated direct-play evidence.
- Latest default 2-seed `yarn browser-direct --strict` passed all observation gates: novice no-legend 2/2 clears, normal 0-legend 0/2 clears while 1-legend and 2-legend each 2/2 clears, intermediate 2-legend 0/2 vs 5-legend 2/2 clears, expert 5-legend 0/2 vs unrestricted 2/2 clears, and master unrestricted 0/2 clears.
- Latest `yarn balance-audit --browser=... --direct=...` passed browser 10R and browser-direct rows, while still reporting the real 2-hour human manual play rows as MISSING when no real `output/manual-balance-playlog.json` is present.
- Re-clarified the map progression semantics after user feedback: a selected map stays fixed for the full 1~40R run, and only clearing the current unlocked map's round-40 boss unlocks the next map. Added an explicit `canUnlockNextStage` policy helper plus edge tests, and changed the new-game map labels to say "선택 후 1~40R 고정".
- Manual play evidence is now separated from examples: `yarn manual-playlog` stamps sessions with `source: "human-playtest"`, the example JSON is marked `example: true`, and `yarn balance-audit --manual=docs/manual-balance-playlog.example.json` excludes those sessions instead of treating them as real 2-hour play evidence.
- Manual play timing is now validated: `yarn manual-playlog` writes both `startedAt` and `endedAt` and rejects mismatched durations, while `yarn balance-audit` only counts non-example sessions whose reported play time matches their timestamp span.
- Manual play metadata is now required for audit time: `yarn manual-playlog` requires result, stage, round, seed, legend count, and max grade, and `yarn balance-audit` only counts sessions with complete metadata plus valid timing toward the 120-minute human-play requirement.
- Manual play coverage now requires meaningful time on every difficulty: `yarn balance-audit` requires at least 12 valid manual minutes for each of novice, normal, intermediate, expert, and master in addition to 120 total minutes and target-outcome rows.
- Clarified the map progression UI and test wording around the intended stage system: the map is chosen once at new-game start, stays fixed through 1~40R including boss rounds, and only a 40R final-boss clear on the current unlocked map grants the next map selection permission.
- Manual target rows now require meaningful target sessions too: each manual outcome proof row must come from a valid non-example session of at least 12 minutes, so short result-only entries cannot satisfy the requested human balance proof while separate filler sessions supply the time.
- Manual play evidence now carries result provenance: result summaries and generated `yarn manual-playlog` commands include `dataVersion` and final-state `stateChecksum`, and `yarn balance-audit` only counts manual sessions with both fields.
- Manual play result checksums are now unique evidence keys: `yarn manual-playlog` rejects duplicate `stateChecksum` entries, and `yarn balance-audit` counts duplicate checksums only once so copied result rows cannot inflate the 2-hour proof.
- Manual play legend metadata is now consistency-checked both ways: `yarn manual-playlog` and `yarn balance-audit` reject sessions where `legends` and `maxGrade` disagree about whether the run had legend-or-better units.
- Map progression UI now says the selected map is fixed for the whole 1~40R run, and that a 40R final-boss clear grants the next map selection permission rather than changing maps between rounds.
- Latest 30-seed `yarn balance --json=/tmp/borandi-balance-current.json` still passes all target bands: 입문자/무전설 100.0%, 일반 0/1/2전설 20.0%/40.0%/56.7%, 중급자 2전설 13.3% vs 5전설 50.0% and unrestricted 100.0%, 고수 5전설 0.0% vs unrestricted 63.3%, 초고수 unrestricted 0.0%.
- Latest default-style 2-seed browser-direct strict sweep passed all observation gates and accumulated 9.95 simulated hours: novice no-legend 2/2 clears, normal 0-legend 0/2 while 1-legend and 2-legend 2/2 clear, intermediate 2-legend 0/2 vs 5-legend 1/2, expert 5-legend 0/2 vs unrestricted 2/2, and master unrestricted 0/2.
- Fixed `yarn browser-balance --screenshots=...` artifact capture by clearing stale result modals between scenarios and before screenshots; rerun passed 7/7 browser 10R gates and verified `intermediateFiveLegend.png` now shows the actual 11R 중급자 5전설 gameplay state.
- Latest combined `yarn balance-audit --balance=/tmp/borandi-balance-current.json --browser=/tmp/borandi-browser-balance-current.json --direct=/tmp/borandi-browser-direct-current.json --manual=docs/manual-balance-playlog.example.json` reports automatic/browser evidence PASS and only the real 2-hour human manual rows as MISSING.
- `yarn manual-playlog --summary` now prints the current valid manual-session minutes, per-difficulty coverage, target outcome rows, and the exact next manual session still needed, so the remaining 2-hour direct-play proof can be filled without reading the full audit table each time.
- Result modals and exported result reports now label which manual balance proof row the just-finished run can satisfy, or say that it only counts toward manual play time, so the 2-hour direct-play pass is less likely to be logged against the wrong target.
- Tools now includes a manual balance evidence guide with the required target sessions and a copyable `yarn manual-playlog --summary` command, keeping the remaining real-play evidence checklist visible inside the game.
- `yarn --silent manual-playlog --summary --json` now emits the same manual evidence status as structured JSON, allowing future checks to consume the remaining human-play proof state without parsing console text.
- Added `yarn balance-proof` to regenerate the CLI balance JSON, browser 10R gate JSON/screenshots, browser-direct strict JSON, and combined audit in one command; this keeps automated evidence reproducible while still leaving the real 2-hour human play rows as the explicit remaining proof.
- Latest `yarn balance-proof` completed end-to-end after the dev-server cleanup fix: CLI balance PASS, browser 10R gate 7/7 PASS, browser-direct strict 5/5 observations PASS over 9.60 simulated hours, and the audit now reports only the real 2-hour human manual play rows as MISSING.
- New-game difficulty buttons now show the exact manual proof target for that difficulty, and the manual balance guide includes both text/JSON summary commands plus a 72-minute target-session-first plan for completing the remaining 120-minute human play proof.
- `yarn manual-playlog --plan` now prints the remaining human-play proof roadmap in order: missing 12-minute target sessions first, then per-difficulty minimum top-ups, then any flexible minutes needed to reach 120 total.
- `yarn test` now includes a `scripts/manual-playlog.test.js` CLI integration test that locks the manual proof plan behavior for empty logs, example logs, and completed 120-minute logs.
- Re-applied the user's corrected stage semantics in UI language: a selected map is a fixed 1~40R run target, and a clear grants next-map selection permission rather than changing maps after each stage/round. Result modals, reports, topbar, render_game_to_text, and new-game map labels now use this framing.
- Added a live `수동증거` topbar timer and matching `render_game_to_text.manualProof` state so real human balance sessions can see when a run has reached the 12-minute minimum required for target evidence rows.
- Added per-difficulty manual proof condition status to the bottom phase label and `render_game_to_text.manualProof`, so human playtesters can see whether the current legend count is still aligned with the needed novice/normal/intermediate/expert/master evidence row.
- Added `yarn manual-playlog --next` and exposed it in the in-game manual proof guide, reducing the 120-minute human-play checklist to the single next session the tester should run.
- Result modals and exported reports now include a combined "record this result, then show next manual session" command, with a copy button, to reduce missed manual-play evidence after a real run.
- Added `yarn manual-playlog --assert`, a failing gate for incomplete human-play evidence, so the 2-hour manual balance proof cannot be accidentally treated as complete while required sessions are still missing.
- Added `yarn balance-audit --assert` plus `yarn balance-proof --require-complete`, so the full proof pipeline can fail closed until the real 120-minute human balance evidence and all automatic/browser gates are present.
- `yarn balance-proof --require-complete` now runs `yarn manual-playlog --assert` as a fast preflight before the expensive automated/browser gates, so missing human-play evidence is reported immediately.
- Added pending manual session markers: `yarn manual-playlog --start`, `--pending`, and `--finish=<id>`. The in-game manual proof guide now exposes the current run's start marker command, and result reports include a finish command, reducing the chance that a real 12+ minute play session is lost if the result command is missed.
- `yarn manual-playlog --summary`, `--plan-json`, and `--next-json` now expose pending start-marker counts so unfinished real-play sessions are visible in the normal manual-proof checklist.
- Added `yarn manual-playlog --finish-latest` and `--finish` without an id as shortcuts for closing the newest pending start marker, reducing friction after a real run ends.
- Hardened the corrected map progression rule at the new-run entry point: the chosen map remains fixed for 1~40R, and any request for a locked map is clamped to the current map selection permission instead of starting an unauthorized map.
- `yarn balance-audit --assert` now fails when `manual-playlog` has unfinished pending start markers, making started-but-unfinished real-play sessions visible in the full balance proof.
- `yarn balance-audit` now includes the next required manual play session in its audit table, so a failing full proof directly tells the tester which 12+ minute target run or time top-up to do next.
- Latest `yarn balance --json=output/current-balance.json` 30-seed run passed all automatic gates: novice/no-legend 100.0%, normal 0/1/2 legend 20.0%/40.0%/56.7%, intermediate 2/5/open 13.3%/50.0%/100.0%, expert 5/open 0.0%/63.3%, master open 0.0%. No stat change was made from this run; the remaining blocker is still real 120-minute manual play evidence.
- `yarn manual-playlog --next` and `--plan` now print a copyable start-marker command template for the next required manual session, with `GAME_SEED_HERE` as the only value to replace after starting the in-game run.
- Added `yarn manual-playlog --start-next --seed=...`, which creates a pending start marker for the next required manual session using the correct difficulty and target-session note automatically.
- The in-game manual balance evidence guide now also shows and copies the `yarn manual-playlog --start-next --seed=GAME_SEED_HERE` template, so the next required human-play session can be marked from the same checklist screen.
- `yarn manual-playlog --start-next` now prints the next target's play/log conditions and a finish command template matched to that target, instead of showing a generic loss/0-legend example that could mis-record normal/intermediate/expert evidence.
- The current-run start marker shown in the manual balance guide now uses the live manual proof target label as its notes value, so exact-seed sessions are tagged with the intended evidence row instead of a generic difficulty note.
- Result modals and exported reports now include a `yarn manual-playlog --finish-latest ...` command alongside the exact-id finish command, so sessions started with `--start-next` can be closed without matching the in-game generated marker id.
- Result modals and exported reports now also include a `finish-latest && yarn manual-playlog --next` command, so a human tester can close the latest marked session and immediately see the next required balance target.
- The live manual-proof timer now shows the remaining time until the 12-minute target in both the top bar and bottom phase label, and `render_game_to_text.manualProof` exposes `remainingSeconds` so browser checks can verify the direct-play evidence timer.
- The bottom phase label now includes the current manual proof target name as well as the condition and remaining time, so a tester can see in-run whether the session is intended for novice no-legend, normal 1~2 legend, intermediate 5+ legend, expert, or master evidence.
- The bottom action bar now has a direct `수동증거` button that opens the current-run manual proof guide, making the exact start-marker command and remaining target commands reachable without digging through the Tools menu during a human playtest.
- Result modals and exported result reports now include a manual proof checklist that separates the 12-minute timer, 40R reach, result, and legend-count criteria, so failed or too-short human playtest runs show exactly why they do not satisfy a target evidence row.
- The manual proof checklist no longer shows a 40R requirement for master difficulty, matching the audit rule that 초고수 only needs a 12+ minute failure record rather than a final-round failure.
- Added a manual-playlog regression test that locks the 초고수 `--start-next` finish template to `ROUND_REACHED` instead of a hard-coded 40R, keeping the CLI aligned with the master manual evidence rule.
- Latest `yarn balance-proof` on the current tree regenerated `output/current-balance.json`, browser 10R screenshots/JSON, browser-direct strict JSON, and `output/balance-audit.md`: automatic/browser evidence PASS with CLI balance novice/no-legend 100.0%, normal 0/1/2 legend 20.0%/40.0%/56.7%, intermediate 2/5/open 13.3%/50.0%/100.0%, expert 5/open 0.0%/63.3%, master open 0.0%; browser direct passed 5/5 observations over 9.77 simulated hours. The audit still reports only the real 120-minute human manual proof rows as MISSING.
- Expert result checklists now show only the evidence row relevant to the finished run's legend count: 5-or-fewer legends checks the 40R failure target, while 6+ legends checks the 40R clear target. This avoids showing an unrelated expert target as `부족` on a valid expert proof run.
- Reworded the map progression UI again to match the intended system exactly: a map is selected only when starting a new game, the selected map stays fixed through the 1~40R run, and clearing the 40R boss only makes the next map selectable on the next new game. The result modal no longer offers an immediate "next map start" action; it opens the new-game map selector instead.
- Fixed modal layering by moving the shared modal root to `document.body` before opening modals, so new-game/result dialogs render above both title and gameplay scenes. Also kept result modals from reopening over the new-game map selector when the player chooses "새 게임" or "새 게임에서 해금된 맵 선택" from an ended run.
- Browser UI verification passed for the clarified stage system: the new-game modal states that the selected map is fixed from 1R through the 40R final boss, the unlock result says the next map is selectable from the next new game, and clicking the unlock action opens the map selector with map 2 selectable instead of immediately switching maps.
- Extracted result-screen manual proof target/checklist logic into `src/core/manualProofResult.ts` and added focused regression tests for the expert manual evidence split: 5-or-fewer legends checks only the 40R failure row, while 6+ legends checks only the 40R clear row. Browser UI verification also confirmed an expert 6+ legend clear result modal shows only the correct row.
- `yarn manual-playlog --next-json` and `--plan-json` now include a `finishTemplate` for each remaining manual proof step, and the text `--next`/`--plan` output prints the same result/round/legend/max-grade finish condition. This lets tooling or a tester verify the next human-play target before starting the 12+ minute session.
- Re-emphasized the requested map-permission stage system in user-facing UI: a map is selected only when starting a new game, remains fixed through the 40R final boss, never changes between rounds or boss kills, and a 40R clear only grants the next map for a later new game. `render_game_to_text` now exposes the same map contract explicitly.
- Latest `yarn balance-proof` on the current tree regenerated `output/current-balance.json`, `output/browser-balance.json`, `output/browser-direct.json`, screenshots, and `output/balance-audit.md`: CLI balance PASS (novice no-legend 100.0%, normal 0/1/2 legend 20.0%/40.0%/56.7%, intermediate 2/5/open 13.3%/50.0%/100.0%, expert 5/open 0.0%/63.3%, master open 0.0%), browser 10R gate 7/7 PASS, and browser-direct strict 5/5 observations PASS over 9.71 simulated hours. The combined audit still correctly reports the real 120-minute human manual proof rows as MISSING because `output/manual-balance-playlog.json` is absent.
- Tightened normal difficulty's no-legend path while preserving the 1~2 legend target, and hardened master again for the latest data-version seed space: normal enemy HP is now x5.1, normal gold multiplier x1.07, and master enemy HP x15.0. Data/app version bumped to 0.8.1 for replay/checksum clarity. Latest 0.8.1 evidence: `yarn balance --seeds=30` PASS with novice/no-legend 100.0%, normal 0/1/2 legend 13.3%/20.0%/53.3%, intermediate 2/5/open 10.0%/40.0%/93.3%, expert 5/open 6.7%/56.7%, master open 0.0%; browser 10R gate 7/7 PASS; browser-direct strict 5/5 PASS over 9.96 simulated hours. The combined audit still reports only the real 120-minute human manual proof rows as MISSING.
- Added focused regression coverage for the adjusted normal-difficulty proof gates: `src/sim/balanceGate.test.ts` now locks the 1-legend minimum clear-rate and 2-legend improvement requirements, and `scripts/balance-audit.test.js` covers the same boundary through `balance-audit --assert`.
- Refreshed current 0.8.1 balance evidence after the normal-gate regression tests: `yarn balance --seeds=30 --json=output/current-balance.json` PASS with novice/no-legend 100.0%, normal 0/1/2 legend 13.3%/20.0%/53.3%, intermediate 2/5/open 10.0%/40.0%/93.3%, expert 5/open 6.7%/56.7%, master open 0.0%. `yarn browser-direct --seeds=2 --strict --json=output/browser-direct.json` also PASS over 10.11 simulated hours: novice no-legend 2/2 clears, normal no-legend 0/2 while 1-legend 0/2 and 2-legend 1/2, intermediate 2-legend 0/2 vs 5-legend 2/2, expert 5-legend 0/2 vs unrestricted 2/2, master unrestricted 0/2. This keeps the requested structure, but the direct-input normal evidence currently reads closer to "2 legends is the practical clear entry" than "1 legend is enough".
- Refreshed `yarn browser-balance --json=output/browser-balance.json --screenshots=output/browser-balance-screenshots` and `yarn balance-audit --balance=output/current-balance.json --browser=output/browser-balance.json --direct=output/browser-direct.json --manual=output/manual-balance-playlog.json`: browser 10R gates remain 7/7 PASS, automatic/browser audit rows PASS, and only the real 120-minute human manual proof rows remain MISSING. Screenshots inspected for normal 2-legend, intermediate 5-legend, and master no-legend states.
- Tuned the late balance pass around direct-play evidence: legendary units received a small attack bump, autoplay now scores legendary/hidden choices with family and role diversity so 1~2 legend and 5+ legend scenarios use less redundant compositions, and expert enemy HP was raised to x10.5 so 5-legends-or-fewer is again a failure condition while unrestricted expert remains clearable. Data/app version is now 0.8.2.
- Latest 0.8.2 evidence: `yarn balance --seeds=30 --json=output/current-balance.json` PASS with novice/no-legend 100.0%, normal 0/1/2 legend 20.0%/30.0%/50.0%, intermediate 2/5/open 13.3%/43.3%/100.0%, expert 5/open 0.0%/86.7%, master open 3.3%. `yarn browser-direct --seeds=2 --strict --json=output/browser-direct.json` PASS over 9.27 simulated hours: novice no-legend 2/2 clears, normal no-legend 0/2 vs 1-legend 1/2 and 2-legend 1/2, intermediate 2-legend 0/2 vs 5-legend 2/2, expert 5-legend 0/2 vs unrestricted 2/2, master unrestricted 0/2. Browser 10R gate remains 7/7 PASS, `yarn test`, `yarn build`, and `yarn check` passed. Combined audit still reports only the real 120-minute human manual proof rows as MISSING.
- Expanded direct-input verification to 4 seeds: `yarn browser-direct --seeds=4 --strict --json=output/browser-direct-4seed.json` PASS over 19.29 simulated browser-play hours. Results: novice no-legend 4/4 clears, normal no-legend 1/4 vs 1-legend 2/4 and 2-legend 2/4, intermediate 2-legend 1/4 vs 5-legend 2/4, expert 5-legend 0/4 vs unrestricted 4/4, master unrestricted 0/4. The 4-seed audit passes all automatic/browser rows; intermediate 5-legend remains the tightest direct-input target, but still outperforms 2-legend on clear rate, average round, and pressure. Real 120-minute human manual proof rows remain MISSING.
- Rejected an intermediate enemy-limit bump from 52 to 53 after a targeted 4-seed browser-direct check: intermediate 2-legend immediately rose to 2/2 clears before the run was stopped, so the change made the "5 legends required" target less true. Reverted to 0.8.2 stats; future intermediate tuning should avoid raising the shared pressure limit and instead target 5-legend composition quality or higher-legend scaling.
- Added 0.8.3 legend command scaling to target the actual legend-count gates without helping no-legend runs: normal gets a capped +8%/+16% attack bonus at 1/2 legend-or-better, while other difficulties start at 5 legend-or-better with +8% per stack up to +24%. Normal enemy HP was raised to x5.2 and master enemy HP to x18.0 to keep no-legend normal and unrestricted master in their target bands. Latest evidence: `yarn balance --seeds=30 --json=output/current-balance.json` PASS with novice/no-legend 100.0%, normal 0/1/2 legend 20.0%/43.3%/80.0%, intermediate 2/5/open 13.3%/63.3%/100.0%, expert 5/open 0.0%/93.3%, master open 3.3%; `yarn browser-direct --seeds=2 --strict --json=output/browser-direct.json` PASS with normal no-legend 0/2 vs 1-legend 1/2 and 2-legend 2/2, intermediate 2-legend 0/2 vs 5-legend 2/2, expert 5-legend 0/2 vs unrestricted 2/2, master 0/2; `yarn browser-balance`, `yarn test`, `yarn build`, `yarn check`, and combined `yarn balance-audit` PASS for automatic/browser rows. Real 120-minute human manual proof remains MISSING.
- Hardened the corrected map-permission stage system after renewed user clarification: the selected map remains fixed for the whole 1~40R run, and profile progression now requires both a cleared run and an actual 40R final-boss kill record before granting the next map for a later new game. Result/report messaging uses the same final-boss-kill condition, and tests cover the case where a bad caller marks 40R cleared without a final boss kill.
- Tightened the intermediate direct-play gate for 0.8.4 by making 중급자 base enemyLimit 40 and granting a +12 legend-command enemy-limit bonus only at 5 legend-or-better; 고수 base enemyLimit is now 40 as well, keeping the static difficulty limit order while still relying on much higher HP to separate expert difficulty. Follow-up direct-input evidence showed normal 1-legend was still too weak and master could occasionally clear, so normal now gets a 1+ legend enemy-limit bonus (+6) alongside the normal-only attack bonus, and master enemyLimit is 30. Latest evidence: `yarn balance --seeds=30 --json=output/current-balance.json` PASS with novice/no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, master open 0.0%; focused `yarn browser-direct --seeds=4 --strict --scenarios=normalNoLegend,normalOneLegend,normalTwoLegend` PASS with normal 0/1/2 legend 1/4, 2/4, 4/4; focused `yarn browser-direct --seeds=4 --strict --scenarios=masterOpen` PASS with master 0/4; full `yarn browser-direct --seeds=2 --strict --json=output/browser-direct.json` PASS with normal 0/1/2 legend 1/2, 1/2, 2/2, intermediate 2/5 legend 0/2 vs 2/2, expert 5/open 0/2 vs 1/2, master 0/2. `yarn browser-balance --json=output/browser-balance.json --screenshots=output/browser-balance-screenshots`, `yarn test`, `yarn build`, `yarn check`, official `$WEB_GAME_CLIENT`, and `yarn balance-audit --balance=output/current-balance.json --browser=output/browser-balance.json --direct=output/browser-direct.json --manual=output/manual-balance-playlog.json` PASS for all automatic/browser rows. Real 120-minute human manual proof remains MISSING.
- Expanded the final 0.8.4 browser direct-input evidence to the full 4-seed scenario set: `yarn browser-direct --seeds=4 --strict --json=output/browser-direct-4seed-current.json` PASS over 17.39 simulated browser-play hours. Results: novice no-legend 4/4 clears, normal 0/1/2 legend 0/4 vs 3/4 vs 4/4, intermediate 2/5 legend 0/4 vs 3/4, expert 5/open 0/4 vs 3/4, master open 0/4. The 4-seed audit using this direct JSON passes all automatic/browser rows and still reports only the real 120-minute human manual proof rows as MISSING.
- Reworded the stage/map progression language after clarification: this is not a map-changing stage chain inside a run. The player chooses one map when starting a new game, plays that same map through 1~40R and the final boss, and a 40R final-boss clear only adds the next map selection permission for a later new game.
- Re-ran the current-code 30-seed balance gate after the map wording commit: `yarn balance --seeds=30 --json=output/current-balance.json` PASS with novice/no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. The combined audit with the 4-seed browser-direct evidence still passes all automatic/browser rows and reports only the real 120-minute human manual proof rows as MISSING. `yarn manual-playlog --plan` shows the remaining proof order: six 12+ minute target sessions first, then 48+ flexible real-play minutes to reach 120 total.
- Fixed the `yarn manual-playlog --start-next` finish-command template so it no longer prints a stale hard-coded `--dataVersion=0.8.0 --stateChecksum=1234abcd`; it now uses explicit placeholders for the result report's actual data version and checksum. Verified with an isolated `/tmp` start-next smoke, `yarn test`, and `yarn check`. The real `output/manual-balance-playlog.json` remains absent, so the requested 120-minute human proof is still incomplete.
- Improved `yarn manual-playlog --next` so the recommended command is now the safer `yarn manual-playlog --start-next --seed=GAME_SEED_HERE`, with the fully expanded `--start ...` form shown only as a direct fallback. `--next-json` now also exposes `startNextCommandTemplate`, and usage examples use result-value placeholders instead of stale sample data. Verified with `yarn manual-playlog --next`, `yarn manual-playlog --next-json`, `yarn test`, and `yarn check`. Real 120-minute manual evidence is still missing.
- Fixed the remaining manual-proof planning edge case where `--next-json` omitted a `startNextCommandTemplate` for the final flexible "총 120분 보충" step after all six target sessions were logged. The final top-up step now recommends `yarn manual-playlog --start-next --difficulty=DIFFICULTY --seed=GAME_SEED_HERE`. Added regression coverage and verified with `yarn test` plus `yarn check`. Real 120-minute human evidence is still missing.
- The in-game manual proof guide now shows an exact current-run `yarn manual-playlog --start-next --difficulty=... --stage=... --seed='...'` command before the expanded fallback start marker, so a human tester can start the next required proof session without replacing `GAME_SEED_HERE` manually. Browser verification on a live dev server confirmed the modal includes the current seed command, plus `yarn test`, `yarn build`, and `yarn check` passed. Real 120-minute human evidence is still missing.
- The current-run `start-next` command in the in-game manual proof guide now also includes `--startedAt='<run start ISO>'`, so delayed marker creation still preserves the actual run start time for the 12+ minute manual evidence requirement. Verified with `yarn test`, `yarn build`, `yarn check`, and a browser modal check confirming the current seed plus `--startedAt` are present. Real 120-minute human evidence is still missing.
- The current-run `start-next` command now also includes the same explicit `--id=...` as the fallback `--start` command, so a marker created through the recommended shortcut can still be finished with the exact result-modal `--finish=<id>` command as well as `--finish-latest`. Browser verification confirmed both visible start commands share the same ID and include `--startedAt`; `yarn test`, `yarn build`, and `yarn check` passed. Real 120-minute human evidence is still missing.
- Extracted manual proof start-command construction into `src/core/manualProofCommands.ts` with focused regression tests, keeping the in-game recommended `start-next` command and fallback `start` command on the same `--id`, `--seed`, and `--startedAt`. Verified with `yarn test`, `yarn build`, `yarn check`, and a Playwright modal check on `http://127.0.0.1:1426/`. Real 120-minute human evidence is still missing.
- `yarn manual-playlog --plan` and `--plan-json` now include the safer sequential `start-next` recommendation for every remaining proof step, while keeping the expanded direct `--start` command separate. Verified with `yarn manual-playlog --plan`, `yarn --silent manual-playlog --plan-json`, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright visual check of the live game screen. Real 120-minute human evidence is still missing.
- `yarn manual-playlog --start-next` finish templates now include `--endedAt=RESULT_ENDED_AT` and explicitly tell the tester to use the result screen's end time, preventing delayed finish commands from recording the wrong session duration. Verified with an isolated `/tmp` start-next smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright visual check. Real 120-minute human evidence is still missing.
- After any `yarn manual-playlog` result save, the CLI now immediately prints the next required manual proof session and recommended `start-next` command, or confirms that no manual sessions remain. Verified with an isolated `/tmp` save smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright visual check. Real 120-minute human evidence is still missing.
- `yarn manual-playlog --assert` and the `yarn balance-proof --require-complete` preflight failure now print the recommended `start-next` command for the next missing manual proof session before any automated gates run. Verified with both failing commands, focused tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright visual check. Real 120-minute human evidence is still missing.
- `yarn manual-playlog --pending` and `--pending-json` now include a finish command template for each pending start marker, using the target proof condition when the marker came from `--start-next`. Verified with isolated `/tmp` pending text/JSON smokes, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright visual check. Real 120-minute human evidence is still missing.
- `yarn manual-playlog --summary` and `--assert` now also show each pending start marker's finish command template, so a started 12+ minute run can be closed from the normal status/failure output without switching commands. Verified with isolated `/tmp` summary/assert smokes, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright visual check. Real 120-minute human evidence is still missing.
- Re-emphasized the requested stage/map permission system in UI and machine-readable browser state: the selected map is fixed for the full 1~40R run, clearing the 40R final boss does not auto-change maps, and the clear only adds the next map as a selectable permission for a later new game. Added a regression test that treats `unlockedStage` as next-new-game selection permission, not a current-run map switch. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright screenshots/`render_game_to_text` checks on `http://127.0.0.1:1433/`.
- `yarn balance-audit` now includes the recommended `yarn manual-playlog --start-next --seed=GAME_SEED_HERE` command in the next missing manual session row, so the full balance audit tells a tester how to start the remaining human evidence session directly. Verified with `yarn test scripts/balance-audit.test.js`, the current 4-seed audit command, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Current audit still passes all automatic/browser rows and still reports the real 120-minute human manual proof rows as MISSING because `output/manual-balance-playlog.json` is absent.
- `yarn manual-playlog --summary` now also prints the recommended `start-next` command for the next required human proof session, so the default status command is enough to start collecting the missing 120-minute direct-play evidence. Verified with `yarn manual-playlog --summary`, `yarn test scripts/manual-playlog.test.js`, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real 120-minute manual evidence remains missing.
- `yarn balance-audit` now reconstructs and prints a finish command template for unfinished manual start markers, including target-specific result/round/legend placeholders when the marker was created by `--start-next`. Verified with a real temporary `--start-next` pending log, `yarn test scripts/balance-audit.test.js`, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real 120-minute manual evidence remains missing.
- Added `balance-proof --require-complete` regression coverage for unfinished manual start markers: the one-command proof must stop before automated gates and show the pending marker finish template, including the target result/round/legend values and `RESULT_ENDED_AT`. Verified with `yarn test scripts/balance-proof.test.js`, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real 120-minute manual evidence remains missing.
- `yarn manual-playlog --summary-json` now includes the next required manual proof session with the same recommended `start-next` command and finish template as `--next-json`, so UI/scripts can consume the missing human-proof path without parsing summary text. Verified with focused manual-playlog tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real 120-minute manual evidence remains missing.
- `yarn manual-playlog --summary`/`--summary-json`/`--plan-json` now expose invalid manual sessions and their rejection reasons, including timing mismatches and duplicate `stateChecksum` values. This keeps externally edited or stale human play logs from silently failing the 120-minute proof. Verified with focused invalid-log regression coverage, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real 120-minute manual evidence remains missing.
- `yarn balance-audit` now carries the same invalid-manual-session diagnosis into the full proof table: invalid timing or duplicate `stateChecksum` rows make `balance-audit --assert` fail with a visible "수동: 무효 세션 없음" row and the exact rejection reason. Verified with focused audit regression coverage, the current audit output, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real 120-minute manual evidence remains missing.
- `yarn manual-playlog --assert` now also fails when the manual log contains invalid sessions, even if enough valid sessions are present. The summary row "수동 로그 무효 세션 없음" keeps the standalone manual-proof gate aligned with `balance-audit --assert`. Verified with focused manual-playlog regression coverage, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real 120-minute manual evidence remains missing.
- Added `balance-proof --require-complete` regression coverage for invalid manual sessions: a log with all required valid sessions plus one invalid timing row must fail in the `manual-playlog --assert` preflight before any expensive automatic/browser balance gates start. Verified with focused balance-proof coverage, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real 120-minute manual evidence remains missing.
- Re-ran the automatic balance gate with 50 seeds and saved ignored local evidence in `output/current-balance-50seed.json` plus `output/balance-audit-50seed.md`: all automatic gates PASS with novice/no-legend 100.0%, normal 0/1/2 legend 6.0%/52.0%/86.0%, intermediate 2/5/open 2.0%/72.0%/100.0%, expert 5/open 0.0%/94.0%, and master open 0.0%. `yarn balance-audit` now labels the real balance seed count instead of hard-coding 30 seeds. The 4-seed browser-direct evidence also PASSes in the 50-seed audit, and only the real 120-minute manual evidence remains missing.
- Expanded the browser direct-input evidence to 6 seeds and saved ignored local evidence in `output/browser-direct-6seed-current.json` plus `output/balance-audit-50seed-6direct.md`: all browser-direct observations PASS over 27.86 simulated hours. Results: novice no-legend 6/6 clears, normal 0/1/2 legend 0/6 vs 4/6 vs 5/6, intermediate 2/5 legend 0/6 vs 5/6, expert 5/open 0/6 vs 5/6, and master open 0/6. This further supports the current stat balance, but it is still automated browser input and does not replace the missing real 120-minute human manual evidence.
- `yarn balance-audit` now requires browser direct-input evidence to include at least 6 seeds, and `yarn balance-proof` defaults `--direct-seeds` to 6. Added regression coverage so a 2-seed direct sample fails with `2/6 seeds`, while the current `output/browser-direct-6seed-current.json` audit row PASSes with `6/6 seeds`. Verified with `yarn test scripts/balance-audit.test.js`, current 50-seed/6-direct audit smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright screenshot/`render_game_to_text` check. Real 120-minute human evidence remains missing.
- `yarn manual-playlog --summary`/`--summary-json`/`--plan-json` now expose remaining valid play minutes and completed target-session count, so the missing 120-minute human proof progress is visible without counting rows manually. Empty logs now report 120.0 minutes remaining and 0/6 target sessions complete. Verified with `yarn test scripts/manual-playlog.test.js`, `yarn manual-playlog --summary`, `yarn --silent manual-playlog --summary-json`, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- `yarn balance-audit` now includes the same remaining manual minutes and target-session completion count in the "사람이 직접 2시간 플레이" row, so the full proof table shows `남은 120.0분, 목표 0/6개 완료` when the real manual log is absent. Verified with `yarn test scripts/balance-audit.test.js`, the current 50-seed/6-direct audit smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- `yarn manual-playlog` and `yarn balance-audit --manual=...` now preserve custom manual log paths in recommended start/finish commands by appending `--out=...` when the audited log is not the default `output/manual-balance-playlog.json`. This prevents real 12+ minute play sessions from being started or finished into the wrong log file during the 120-minute proof pass. Verified with focused manual-playlog/audit tests, custom-path CLI smokes, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- `yarn manual-playlog` now auto-links a direct result save to a matching pending start marker when difficulty, stage, seed, and startedAt match exactly, closing that pending marker even if the tester uses the result screen's plain log command instead of `--finish`/`--finish-latest`. This prevents a real session from being recorded while leaving a stale pending marker that would fail the audit. Verified with a new regression test, a temporary CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- `yarn manual-playlog` now reports whether a linked `--start-next` marker's intended target row was satisfied by the saved result. A failed/short/wrong-result run is still preserved as real play time, but the CLI now prints that the target row is still missing instead of letting the tester assume the target proof was completed. Verified with pass/fail regression coverage, a temporary CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- `yarn manual-playlog` result-save output now also prints remaining valid play minutes and target-session progress, for example `목표 세션: 1/6개 완료, 남은 5개`, using the same summary gate as `--summary-json`. This makes each completed real session immediately show how much of the 120-minute proof remains. Verified with focused manual-playlog coverage, a temporary CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- Re-locked the requested map progression wording and browser state around the user's clarified model: choose one map at new-game start, keep that map for rounds 1~40 including the final boss, and only add the next map as a selection permission for a later new game. The new-game modal, title subtitle, start toast, result permission text, `render_game_to_text`, and engine replay regression now all state/check that the current run map does not change. Also blocked duplicate `yarn manual-playlog --start-next` markers while a pending start marker exists, so manual proof sessions stay sequential. Verified with focused engine/settings/manual-playlog tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright modal/gameplay screenshots plus `render_game_to_text` checks. Real `output/manual-balance-playlog.json` evidence remains missing.
- Re-checked the latest committed balance numbers with `yarn balance --seeds=50 --json=/tmp/borandi-balance-latest.json`; all five automatic target gates still PASS. Results: novice no-legend 100.0%, normal 0/1/2 legend 6.0%/52.0%/86.0%, intermediate 2/5/open 2.0%/72.0%/100.0%, expert 5/open 0.0%/94.0%, and master open 0.0%. `yarn manual-playlog --summary` still reports no real `output/manual-balance-playlog.json`, 0.0/120.0 valid minutes, and 0/6 target sessions, so the requested real two-hour direct-play proof remains incomplete.
- `yarn manual-playlog --help` now exits successfully and documents the JSON shortcut aliases (`--summary-json`, `--plan-json`, `--next-json`, `--pending-json`), and the in-game manual proof guide now copies the shorter `yarn --silent manual-playlog --summary-json` command. This keeps the real 120-minute evidence collection commands consistent across CLI help, README, and the game UI. Verified with focused manual-playlog coverage, CLI help/summary-json smokes, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright screenshot of the manual proof guide. Real `output/manual-balance-playlog.json` evidence remains missing.
- Regenerated the browser direct-input evidence with `yarn browser-direct --seeds=6 --strict --json=output/browser-direct.json --screenshots=output/browser-direct-shots`, covering 9 scenarios and 26.96 simulated browser-input hours. All direct observations PASS: novice no-legend 100.0%, normal 0/1/2 legend 0.0%/66.7%/66.7%, intermediate 2/5 legend 0.0%/100.0%, expert 5/open 0.0%/83.3%, and master open 0.0%. A follow-up `yarn balance-audit --manual=output/manual-balance-playlog.json --assert` now reports all automatic/browser rows PASS and only the real human manual rows MISSING. Real `output/manual-balance-playlog.json` evidence remains missing.
- Added `yarn manual-playlog --preflight` as a start-of-session readiness check for the real two-hour manual pass. It exits successfully when there are no invalid sessions or unfinished start markers and prints the next recommended `start-next` command; it exits nonzero with finish templates when a pending marker must be closed first. The README and in-game manual proof guide now expose/copy this command. Verified with focused preflight tests, `yarn manual-playlog --preflight`, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright screenshot of the manual proof guide. Real `output/manual-balance-playlog.json` evidence remains missing.
- `yarn manual-playlog --preflight-json` now exposes the same start-of-session readiness check as structured JSON, including `canStart`, `blockingReasons`, remaining manual minutes, target-session progress, the next start command, and pending finish templates. README and the in-game manual proof guide now show/copy the JSON command. Verified with focused preflight-json tests, CLI preflight-json/help smokes, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright screenshots of the manual proof guide. Real `output/manual-balance-playlog.json` evidence remains missing.
- `yarn manual-playlog` and `yarn balance-audit` now reject manual sessions whose `dataVersion` does not match the current `src/data/version.ts` `DATA_VERSION`, so old balance-version play logs cannot satisfy the current 120-minute proof. `--summary-json`/`--preflight-json` expose `currentDataVersion` and stale sessions show an explicit invalid-session reason. Verified with focused stale-version regression tests, `yarn --silent manual-playlog --preflight-json`, the current failing manual audit, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- Automatic/browser balance evidence now carries and audits the current `DATA_VERSION`: `yarn balance`, `yarn browser-balance`, and `yarn browser-direct` JSON outputs include `dataVersion`, and `yarn balance-audit` fails stale or missing evidence versions before treating those rows as current proof. Regenerated local ignored evidence for 0.8.4: 30-seed balance PASS (novice 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, master 0.0%), browser 10R gates 7/7 PASS, browser-direct 6-seed observations 5/5 PASS over 27.63 simulated hours. Current audit now shows all automatic/browser version and balance rows PASS; only real `output/manual-balance-playlog.json` human evidence remains missing.
- The in-game manual proof guide now displays the current `DATA_VERSION` and explicitly tells the tester to use the result screen's actual `--dataVersion`, `--stateChecksum`, and `--endedAt` values when recording or finishing a session. `render_game_to_text` also exposes `dataVersion`, so browser checks can verify the visible proof guide and machine-readable run state agree. Verified with focused manual proof tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright screenshot/text-state check of the manual proof modal. Real `output/manual-balance-playlog.json` evidence remains missing.
- `yarn manual-playlog` now rejects stale `--dataVersion` values immediately during direct saves and `--finish`/`--finish-latest`, instead of accepting the session and only marking it invalid later in summary/audit output. README examples now use `RESULT_DATA_VERSION`, `RESULT_CHECKSUM`, and `RESULT_ENDED_AT` placeholders rather than stale literal versions. Verified with stale-version save/finish regression tests, a failing stale-version CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- Manual result metadata now rejects impossible round/result combinations: direct saves and finish commands fail immediately for `clear` before 40R or any round above 40, and external logs with those impossible results are marked invalid by `manual-playlog` and `balance-audit`. Verified with focused save/summary/audit regression tests, a failing `clear 39R` CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/`render_game_to_text` check. Real `output/manual-balance-playlog.json` evidence remains missing.
- New-game map selection now treats newly unlocked maps strictly as permission: after clearing a map, reopening the new-game modal keeps the previous/current map selected by default instead of auto-selecting the newest unlocked map. The newly unlocked map is visible and selectable, but the player must explicitly choose it for the next run. Verified with focused settings/engine tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright screenshots/state checks.
- `render_game_to_text` now exposes the current run seed plus current-seed `yarn manual-playlog --start` and `--start-next` commands under `manualProof.commands`, so browser checks and testers can verify the real seed/startedAt command without relying on `GAME_SEED_HERE` templates. Verified with focused manual proof command tests, `yarn build`, `yarn test`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/state check. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `render_game_to_text` now also exposes the current state checksum and `manualProof.evidenceFields` (`difficulty`, `stage`, `seed`, `dataVersion`, `currentStateChecksum`, `startedAt`) so browser tooling can cross-check the exact values needed for manual evidence commands. Verified with focused tests, `yarn build`, `yarn test`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/state check. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Ended runs now keep a stable `runEndedAt` timestamp and expose the final `yarn manual-playlog` result command as `render_game_to_text.manualProof.commands.result`, matching the result modal's command format. Verified with focused tests, `yarn build`, `yarn test`, `yarn check`, the official web-game client, and a Playwright ended-state screenshot/state check. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Ended-run text state now exposes the full result-command set under `manualProof.commands`: direct result save, direct save plus `--next`, exact pending-marker finish, finish-latest, and finish-latest plus `--next`. Verified with `yarn build`, `yarn test`, `yarn check`, the official web-game client, and a Playwright ended-state screenshot/state check. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --dry-run` now validates direct result saves and `--finish`/`--finish-latest` result commands without writing the manual log or closing pending start markers, printing a session JSON preview instead. Verified with focused dry-run regressions, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright screenshot/state check. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Result modals, exported result reports, README examples, and `render_game_to_text.manualProof.commands` now expose `--dry-run` variants for direct result saves, exact pending-marker finish, and finish-latest, with separate headings for dry-run validation versus real log writes. Verified with focused command tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright ended-result screenshot/state check. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --start` and `--start-next` now refuse to create new pending start markers when the manual log already contains invalid sessions, forcing the tester to fix INVALID timing/metadata/checksum rows before adding more real 120-minute evidence. Verified with focused start-block regression coverage, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright title-screen screenshot/state check. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --pending`, `--pending-json`, `--summary`, and `--preflight` now show each pending start marker's elapsed time, 12-minute target, remaining minutes, and whether the target length is ready before finish. This makes an active human proof run easier to close at the right time without guessing from timestamps. Verified with focused pending-timer tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright title-screen screenshot/state check. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Reworded and surfaced the intended map-permission system again: the player chooses one map at new-game start, that map stays fixed through 1~40R and the final boss, and clearing the 40R boss only grants the next map as a selectable permission for a later new game. The new-game modal now shows the current selectable map range, locked maps say "권한 없음", the result modal/report separate "맵 진행" from "맵 권한", the topbar shows current map permission range, and `render_game_to_text` exposes a direct progression contract. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright modal/gameplay text-state checks on `http://127.0.0.1:1452/`.
- The in-game topbar now shows the current manual proof condition next to the 12-minute proof timer, using green/warn/danger coloring for satisfied, pending, or target-broken conditions. This makes real manual balance runs easier to monitor while playing instead of checking only the bottom actionbar or result screen. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/state check on `http://127.0.0.1:1453/`. Re-ran `yarn balance --seeds=30 --json=/tmp/borandi-balance-current.json`: all five gates PASS with novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- The topbar `수동증거` and `증거조건` stats are now clickable shortcuts to the manual balance evidence modal, so a tester can open the current-seed `start`/`start-next` marker commands without moving down to the actionbar during a run. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay click check/screenshot on `http://127.0.0.1:1454/` confirming the modal opens with the current seed and preflight command. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports canStart=true, 120.0 minutes remaining, and 0/6 target sessions.
- The manual balance evidence modal now shows a `현재 판 목표 상태` table before the start-marker commands, including the current difficulty, current legend/hidden count, target row, and current condition status. This reduces the chance of spending a 12+ minute real session on the wrong condition before checking the result screen. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright modal screenshot/text check on `http://127.0.0.1:1455/` confirming a normal no-legend run shows `일반 1~2전설 40R 클리어` and `전설 1~2 필요`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports canStart=true, 120.0 minutes remaining, and 0/6 target sessions.
- Runs now fire a one-time in-game toast when the wall-clock manual proof timer reaches 12 minutes, including the current proof condition status and a reminder to record the result log after the run. `render_game_to_text.manualProof.readyNotified` exposes this state for browser checks, and DEV builds include an `ageRunForManualProof` hook for verification only. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/text-state check on `http://127.0.0.1:1456/` confirming no toast at 719s and a `수동증거 12분 충족` toast at 721s. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports canStart=true, 120.0 minutes remaining, and 0/6 target sessions.
- Result modals now show a top proof banner before the result stats: green when all manual target checks pass, or warning when the run is too short or does not match the target row. This makes short 12-minute-missing runs visibly invalid before the tester copies any log command. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright result-modal screenshot/text check on `http://127.0.0.1:1457/` confirming a short normal loss shows `수동 목표 증거 미충족` and the 12-minute reason. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports canStart=true, 120.0 minutes remaining, and 0/6 target sessions.
- New-game map selection now separates permission from automatic progression more explicitly: a normal new game keeps the current map selected even when later maps are unlocked, while the result screen's newly unlocked-map button only preselects that next map in the new-game modal for a later run. The current run still never changes maps after a clear. Verified with focused settings coverage, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright new-game modal screenshot/state check on `http://127.0.0.1:1458/`.
- Pending manual-playlog start markers now print a `--dry-run` finish template before the real finish template in start, pending, summary, and preflight output. This pushes the real 12+ minute proof flow through validation before writing or closing evidence rows. Verified with focused manual-playlog tests, a temporary `--start-next` CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright title/new-game screenshot/state check on `http://127.0.0.1:1459/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports canStart=true, 120.0 minutes remaining, and 0/6 target sessions.
- `yarn balance-audit` now also shows a `dryRunFinish=... --dry-run` command next to the real `finish=...` command when a manual start marker is still pending, so audit failures can be resolved through validation before writing evidence. README now documents that pending/audit finish guidance includes dry-run first. Verified with focused balance-audit coverage, a temporary pending-log audit smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright title/new-game screenshot/state check on `http://127.0.0.1:1460/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports canStart=true, 120.0 minutes remaining, and 0/6 target sessions.
- Re-ran the automatic balance gate with a larger 100-seed sample and saved the local ignored report at `/tmp/borandi-balance-100seed.json`; all five gates PASS. Results: novice no-legend 100.0%, normal 0/1/2 legend 11.0%/49.0%/86.0%, intermediate 2/5/open 2.0%/70.0%/100.0%, expert 5/open 0.0%/97.0%, and master open 0.0%. This supports the current stat balance and no stat tuning was needed from this sample, but it is still automated evidence and does not replace the missing real 120-minute direct-play proof.
- `yarn manual-playlog --start` and `--start-next` now reject the `GAME_SEED_HERE` placeholder seed instead of creating a useless pending marker, forcing the tester to replace the template with the actual seed from the game screen/current run state before starting a real evidence session. README documents the placeholder guard. Verified with focused manual-playlog coverage, a failing placeholder CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright title/new-game screenshot/state check on `http://127.0.0.1:1461/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports canStart=true, 120.0 minutes remaining, and 0/6 target sessions.
- `yarn manual-playlog` result saves and finish commands now reject template result placeholders (`GAME_SEED_HERE`, `RESULT_DATA_VERSION`, `RESULT_CHECKSUM`, `RESULT_ENDED_AT`) with explicit "use the real result value" errors before writing or closing evidence rows. README documents that result placeholder values must be replaced from the result screen. Verified with focused manual-playlog coverage, a failing placeholder result CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright title/new-game screenshot/state check on `http://127.0.0.1:1462/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports canStart=true, 120.0 minutes remaining, and 0/6 target sessions.
- The in-game manual balance evidence modal now uses the active run's current-seed `start-next` command in the status/copy section instead of the `GAME_SEED_HERE` template when a game is already running. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright modal text/screenshot check on `http://127.0.0.1:1463/` confirming `--seed='REAL-SEED-UI'` appears and `GAME_SEED_HERE` does not. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --start` and `--start-next` now support `--dry-run`, validating the start marker, next required target, and finish templates without writing pending evidence. The in-game manual evidence modal now shows/copies current-seed start-marker dry-run commands before the real start commands, so a tester can catch wrong difficulty/seed/pending-state issues before spending a 12+ minute proof run. Verified with new manual-playlog regressions, a temporary CLI dry-run smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright modal screenshot/text check on `http://127.0.0.1:1464/` confirming current-seed `--dry-run` commands appear and `GAME_SEED_HERE` does not. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --next`, `--plan`, `--summary`, and `--preflight-json` now expose start-marker dry-run commands alongside the real start marker templates (`startCommandDryRunTemplate`, `startNextDryRunCommandTemplate`, and `nextStartDryRunCommandTemplate`). The text output now prints "추천 시작 검증" before "추천 시작 마커" so the real 12+ minute proof flow starts with validation by default. Verified with expanded manual-playlog regressions, a temporary `--next`/`--preflight-json` CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright title-screen screenshot/state check on `http://127.0.0.1:1465/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `render_game_to_text.manualProof.commands` now includes `startDryRun` and `startNextDryRun` alongside the real `start`/`startNext` commands, using the current run seed and startedAt. This lets browser checks compare the visible current run against the CLI validation-first manual proof flow before a real 12+ minute session starts. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright game-state/screenshot check on `http://127.0.0.1:1466/` confirming current-seed dry-run commands appear while the real start commands remain non-dry-run. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-audit` now includes the start-marker dry-run command in the "다음 수동 플레이 세션" evidence row before the real start marker, so the final audit report also points testers through validation before starting a 12+ minute proof run. Verified with focused balance-audit regression coverage, an audit CLI smoke showing `추천 시작 검증: yarn manual-playlog --start-next --seed=GAME_SEED_HERE --dry-run`, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright title-screen screenshot/state check on `http://127.0.0.1:1467/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Result-screen map progression now matches the clarified permission model more strictly: clearing the 40R final boss only unlocks the next map as a selectable permission, and the result modal no longer shows a special button that preselects the newly unlocked map. The only forward action is "새 게임", where the player explicitly chooses any permitted map. Verified with focused settings/engine tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright checks/screenshots on `http://127.0.0.1:1468/` confirming stage 2 can be manually selected when unlocked, stage 3 remains locked, the current run reports `fixedForRun: true`, and a 40R clear unlocks `unlockedStage: 2` without any "해금된 맵" auto-next button.
- `yarn manual-playlog --assert` and therefore `yarn balance-proof --require-complete` now print the start-marker dry-run command before the real start marker when manual proof is incomplete. Verified with focused manual-playlog/balance-proof tests, a `balance-proof --require-complete` smoke that stops before automatic gates, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/state check on `http://127.0.0.1:1469/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran the current automatic/browser balance evidence after the proof-gate and map-permission commits. `yarn balance --seeds=30 --json=/tmp/borandi-balance-latest.json` PASS: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, master open 0.0%. `yarn browser-balance --json=/tmp/borandi-browser-balance-latest.json --screenshots=/tmp/borandi-browser-balance-shots` PASS 7/7, and `yarn browser-direct --seeds=6 --strict --json=/tmp/borandi-browser-direct-6seed-latest.json` PASS 5/5 observations over 26.96 simulated hours. The combined audit now reports automatic/browser PASS, but real human evidence is still missing: `output/manual-balance-playlog.json` does not exist, manual time remains 0.0/120.0 minutes, target rows remain 0/6, and the next required real session is a 12+ minute novice no-legend 40R clear.
- Pending manual-playlog start markers now block `--summary` and `--next` from showing a new `--start-next` recommendation until the pending marker is finished. The output prioritizes the pending marker's dry-run finish command and real finish command, and `--next-json` exposes `blockedByPendingStartMarkers: true` with `next: null`. This prevents a real tester from starting a second 12+ minute proof session while the previous start marker is still open. Verified with focused manual-playlog coverage, a temporary pending-log CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright gameplay screenshot/state check on `http://127.0.0.1:1470/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- README manual-playlog instructions now document the pending-marker priority rule: when a start marker is still open, `--summary`/`--next` suppress new `--start-next` recommendations and `--next-json` returns `blockedByPendingStartMarkers: true` with `next: null`. Verified with focused manual-playlog tests and a temporary pending-log `--next` CLI smoke. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn browser-direct` now defaults to 6 seeds, matching the `balance-audit` minimum browser-direct evidence requirement and the existing `balance-proof` default. README now shows `--seeds=6` and documents the 6-seed audit floor. Verified with a short `browser-direct` default-seed smoke on the dev server (1 scenario, 1 max round, JSON recorded 6 seeds), `yarn test`, `yarn build`, `yarn check`, the official web-game client, and screenshot inspection. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran a 50-seed automatic balance sample on the current tree: all target gates still PASS with novice/no-legend 100.0%, normal 0/1/2 legend 6.0%/52.0%/86.0%, intermediate 2/5/open 2.0%/72.0%/100.0%, expert 5/open 0.0%/94.0%, and master open 0.0%. Combined audit with the latest browser-balance and 6-seed browser-direct evidence reports automatic/browser PASS and only the real manual proof rows as MISSING. No stat tuning was made from this sample. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --start` and `--start-next` dry-run output now explicitly warns that the shown finish command uses a temporary dry-run id, and that a real 12+ minute session must first save the start marker without `--dry-run` and later use that saved id or `yarn manual-playlog --pending`. This reduces the chance that a real proof run is played but then finished against a non-existent dry-run marker. Verified with focused manual-playlog tests, a `--start-next --dry-run` CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a browser screenshot smoke on `http://127.0.0.1:1472/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --preflight` now prints an explicit five-step real-session workflow after the start commands: start the right in-game run, replace `GAME_SEED_HERE`, dry-run validate, save the start marker without `--dry-run`, play 12+ minutes, then finish with result-screen data. `--preflight-json` exposes the same `startWorkflow` array for tooling. Verified with focused manual-playlog tests, text/JSON preflight smokes, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a browser screenshot smoke on `http://127.0.0.1:1473/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- The in-game `수동 밸런스 증거` modal now shows the same five-step real-session workflow next to the current-seed start marker commands, so a tester can follow the proof flow without leaving the game. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a Playwright DOM/screenshot check on `http://127.0.0.1:1474/` confirming the workflow text and current run seed appear in the modal. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Target-specific `yarn manual-playlog --start-next` recommendations now include the expected `--difficulty=...` value in `--preflight`, `--next`, `--summary`, `--plan`, `balance-audit`, and `balance-proof` output. This makes a wrong-difficulty real run fail the dry-run/start validation instead of silently creating a marker for the next target with only the seed changed. Verified with focused manual-playlog/audit/proof tests, CLI preflight/plan/audit smokes, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and a browser screenshot smoke on `http://127.0.0.1:1475/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports 0.0/120.0 minutes and 0/6 target sessions.
- Re-locked the clarified map-permission model after the latest correction: README now states the player chooses one map at new-game start, plays that same map through the 40R final boss, and only gains next-map selection permission for a later new game. Added a regression test proving the unlock write does not mutate the current `Game.state.stageId`.
- Aligned the remaining manual-proof documentation/fallback examples with the target-specific `start-next` flow: README, `manual-playlog --help`, and the out-of-game manual proof modal fallback now show `--difficulty=...` instead of a seed-only `--start-next`, so real direct-play markers are started against the intended difficulty.
- Moved the most important manual-proof start commands to the top of the in-game `수동 밸런스 증거` modal: when a run is active, the current-seed `start-next --dry-run` and real `start-next` marker now appear before the longer reference sections, reducing friction before the missing real 120-minute direct-play pass.
- Re-ran the current balance evidence without changing stats or stage logic. `yarn balance --seeds=50 --json=/tmp/borandi-balance-50seed-20260620.json` PASSed all target gates: novice no-legend 100.0%, normal 0/1/2 legend 6.0%/52.0%/86.0%, intermediate 2/5/open 2.0%/72.0%/100.0%, expert 5/open 0.0%/94.0%, and master open 0.0%. The official web-game Playwright client passed against `http://127.0.0.1:1479/`. `yarn browser-balance --json=/tmp/borandi-browser-balance-20260620.json --screenshots=/tmp/borandi-browser-balance-20260620-shots` PASSed 7/7 browser gates, and `yarn browser-direct --seeds=6 --strict --json=/tmp/borandi-browser-direct-6seed-20260620.json --screenshots=/tmp/borandi-browser-direct-6seed-20260620-shots` PASSed all direct-input observation gates over 27.21 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 0/6 vs 3/6 vs 6/6, intermediate 2/5 legend 0/6 vs 6/6, expert 5/open 0/6 vs 5/6, and master open 0/6. Combined `yarn balance-audit` reports automatic/browser evidence PASS and only the real human manual proof rows as MISSING. `yarn --silent manual-playlog --preflight-json` still reports no real `output/manual-balance-playlog.json`, 0.0/120.0 valid minutes, 0/6 target sessions, and the next required session as a 12+ minute novice no-legend 40R clear.
- `yarn manual-playlog` now supports `--source=human-playtest|codex-direct-playtest`, so future direct-play evidence can honestly distinguish a person-run session from a Codex-controlled browser direct-play session instead of recording both as `human-playtest`. Pending start markers preserve the source through `--finish`/`--finish-latest`, start output prints the selected source, and `manual-playlog`/`balance-audit` evidence rows include the source next to each counted session. Verified with focused source-preservation coverage, a `--source=codex-direct-playtest --dry-run` CLI smoke, `yarn test`, `yarn build`, `yarn check`, the official web-game Playwright client, and a screenshot/state check on `http://127.0.0.1:1480/`. No real 12+ minute session was written in this step; `output/manual-balance-playlog.json` is still absent and the 120-minute direct-play proof remains incomplete.
- Tightened the source split so `codex-direct-playtest` sessions can no longer satisfy the "사람이 직접 2시간 플레이" or manual target outcome rows. `manual-playlog --summary-json` now keeps `totalMinutes`/target rows based on `human-playtest` only while exposing `validSessionCount`, `validHumanSessionCount`, `codexDirectSessionCount`, and `codexDirectMinutes`; `balance-audit` applies the same human-only counting and includes codex-direct minutes only as a separate diagnostic. Added regression coverage proving a complete 120-minute codex-direct-only log still fails the human manual proof and leaves the next required session at novice no-legend 40R. Verified with focused manual-playlog/balance-audit tests, `yarn test`, `yarn build`, `yarn check`, the official web-game Playwright client, `yarn --silent manual-playlog --preflight-json`, and a screenshot/state check on `http://127.0.0.1:1481/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, codex-direct 0.0 minutes, and 0/6 target sessions.
- Clarified the human-vs-Codex direct-play split in CLI output and README. `yarn manual-playlog --preflight` now labels the counted time as `유효 사람 플레이 시간`, and if any `codex-direct-playtest` sessions exist it prints a separate `Codex 직접 조작 보조 시간` line that explicitly says it is not included in the human 120-minute proof. README now documents `--source=codex-direct-playtest` as a supplemental browser-control evidence source that does not replace `human-playtest`. Verified with focused manual-playlog coverage, `yarn test`, `yarn build`, `yarn check`, the official web-game Playwright client, `yarn --silent manual-playlog --preflight-json`, and screenshot/state inspection on `http://127.0.0.1:1482/`. Current preflight still reports no real `output/manual-balance-playlog.json`, human 0.0/120.0 minutes, codex-direct 0.0 minutes, and 0/6 target sessions.
- Re-tightened the stage system to match the latest correction: stage/map selection is made only at new-game start, the selected map remains fixed through 1~40R and the final boss, and a 40R boss clear only grants next-map selection permission for a later new game. Removed the remaining preferred-stage path from the new-game modal so even a caller cannot preselect a newly unlocked next map automatically; the player must choose the map manually in the new-game modal. Verified with focused settings tests, `yarn test`, `yarn build`, `yarn check`, the official web-game Playwright client, and Playwright screenshots/state checks on `http://127.0.0.1:1483/` confirming an unlocked 2번 맵 leaves 1번 selected by default while a manual 2번 click starts stage 2.
- `yarn balance-audit` now prints a separate `Codex 직접 조작 보조 증거 분리` PASS row so Codex-controlled browser direct-play sessions are visible as supplemental evidence but explicitly excluded from the `human-playtest` 120-minute total. README documents the row, and regression coverage proves a 120-minute codex-direct-only log still fails the human manual proof. Verified with focused balance-audit tests, `yarn test`, `yarn build`, `yarn check`, a `balance-audit --manual=docs/manual-balance-playlog.example.json` CLI smoke, the official web-game Playwright client, and screenshot/state inspection on `http://127.0.0.1:1484/`. Real `output/manual-balance-playlog.json` evidence remains missing: preflight still reports human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran current balance evidence on the latest tree. A 30-seed CLI gate passed all five target bands: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. A 100-seed attempt was stopped after confirming novice 100.0%, normal 0 legend 11.0%, normal 1 legend 49.0%, and normal 2 legend 86.0% because the full run was too slow for the current turn. Browser 10R gate on `http://127.0.0.1:1485/` passed 7/7 with screenshots inspected for intermediate 5-legend success and master no-legend collapse. Combined audit with the latest 30-seed balance, browser-balance, and existing 6-seed browser-direct evidence reports automatic/browser PASS and only real human manual proof rows as MISSING. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran the latest 6-seed `yarn browser-direct --strict` pass on `http://127.0.0.1:1486/` without changing stats. The browser direct-input sampler passed all 5 observations over 27.34 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 2/6 vs 2/6 vs 5/6 with better pressure at 2 legends, intermediate 2/5 legend 0/6 vs 5/6, expert 5/open 0/6 vs 5/6, and master open 0/6. Screenshots were inspected for normal 2-legend 40R clear and master open early collapse. Combined audit using the latest balance, browser-balance, and browser-direct JSON reports automatic/browser PASS and only real human manual proof rows as MISSING. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-proof` now passes `--screenshots=output/browser-direct-shots` to the browser-direct sampler by default, with an override via `--direct-screenshots=...`, so the one-command evidence refresh captures visual artifacts for both browser 10R and browser direct-input gates. Verified with focused proof/audit tests, `yarn test`, `yarn build`, `yarn check`, a fake-yarn command capture confirming the browser-direct screenshots argument, the official web-game Playwright client, and a runtime screenshot/state check on `http://127.0.0.1:1488/`. A 1-seed balance-proof smoke intentionally stopped at the under-sampled CLI gate, so full proof refresh still requires the normal 30/6 seed settings. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran the latest 6-seed browser direct-input sampler on `http://127.0.0.1:1490/` without changing stats. `yarn browser-direct --url=http://127.0.0.1:1490/ --seeds=6 --strict --json=output/browser-direct-latest-20260621.json --screenshots=output/browser-direct-latest-20260621-shots` passed all 5 observations over 27.06 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 0/6 vs 2/6 vs 4/6, intermediate 2/5 legend 0/6 vs 5/6, expert 5/open 0/6 vs 5/6, and master open 0/6. Screenshots were inspected for normal 2-legend pressure/failure, intermediate 5-legend clear, and master early collapse. Combined `yarn balance-audit` with the latest direct JSON reports automatic/browser PASS and only real human manual proof rows as MISSING. Since every direct-input gate still matches the requested balance bands, no stat tuning was made. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran a full 100-seed CLI balance sample on the current tree and saved the ignored local report at `output/current-balance-100seed-20260621.json`. All five gates passed: novice no-legend 100.0%, normal 0/1/2 legend 11.0%/49.0%/86.0%, intermediate 2/5/open 2.0%/70.0%/100.0%, expert 5/open 0.0%/97.0%, and master open 0.0%. Combined `yarn balance-audit --balance=output/current-balance-100seed-20260621.json --browser=output/browser-balance.json --direct=output/browser-direct-latest-20260621.json --manual=docs/manual-balance-playlog.example.json` reports automatic/browser PASS and only real human manual proof rows as MISSING. The official web-game client passed on `http://127.0.0.1:1491/`, representative direct-input screenshots were inspected, and `yarn test`, `yarn build`, and `yarn check` passed. No stat tuning was made because the larger sample still matches the requested balance bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn browser-direct` run JSON now preserves per-run final `dataVersion`, `stateChecksum`, difficulty, fixed stage metadata, endedAt, and a ready-to-copy `codexDirectPlaylogCommand` using `--source=codex-direct-playtest`. This does not count toward the human 120-minute proof, but it makes Codex-controlled direct-input sessions traceable through the same manual-log validator as supplemental evidence. Verified with a short browser-direct smoke on `http://127.0.0.1:1492/`, `manual-playlog --dry-run` against the generated values, screenshot inspection, and the official web-game client. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn browser-direct` now supports `--codex-log=...` to write a `manual-playlog`-compatible log containing the same `codex-direct-playtest` sessions. A short smoke on `http://127.0.0.1:1493/` wrote `/tmp/borandi-codex-direct-playlog-smoke.json`; `yarn balance-audit --manual=/tmp/borandi-codex-direct-playlog-smoke.json` reported `Codex 직접 조작 보조 증거 분리` PASS with codex-direct 0.1 minutes while keeping human 0.0/120.0 minutes MISSING. Screenshot inspection confirmed the run state remained visible and map-fixed. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-proof` now passes `--codex-log=output/codex-direct-playlog.json` to `browser-direct` by default, with a `--direct-codex-log=...` override. This makes the one-command proof refresh regenerate CLI/browser evidence, direct screenshots, and Codex direct-play supplemental logs together while still leaving the human 120-minute proof separate. Verified with focused balance-proof regression coverage, a deliberately under-sampled 1-seed proof smoke that stopped at the CLI gate before browser steps, `yarn test`, `yarn build`, `yarn check`, `manual-playlog --preflight-json`, the official web-game client, and screenshot/state inspection on `http://127.0.0.1:1496/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran the full `yarn balance-proof` evidence refresh on the current tree. The 30-seed CLI gate passed all target bands: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. Browser 10R gates passed 7/7. Browser direct-input gates passed 5/5 across 54 Codex-controlled sessions and 27.23 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 0/6 vs 5/6 vs 6/6, intermediate 2/5 legend 0/6 vs 5/6, expert 5/open 0/6 vs 5/6, and master open 0/6. `output/codex-direct-playlog.json` contains 54 `codex-direct-playtest` sessions, and an audit using it as `--manual` confirms those 1633.5 minutes remain excluded from human proof. Representative direct screenshots and the official web-game client state/screenshot on `http://127.0.0.1:1497/` were inspected; `yarn test`, `yarn build`, `yarn check`, and `manual-playlog --preflight-json` passed. No stat tuning was made because every automatic/browser/direct gate still matches the requested bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-audit` now accepts `--codex=output/codex-direct-playlog.json` and, when using the default manual log path, loads that Codex direct-play log by default for the `Codex 직접 조작 보조 증거 분리` row. `yarn balance-proof` passes the generated `--direct-codex-log` path into the final audit, so the one-command proof report now shows 54 codex-direct sessions and 1633.5 supplemental minutes while still keeping human proof at 0.0/120.0 minutes. Custom manual-log audits stay isolated unless `--codex` is explicitly provided. Verified with focused audit/proof regressions, a current evidence audit smoke, `yarn test`, `yarn build`, `yarn check`, `manual-playlog --preflight-json`, the official web-game client, and screenshot/state inspection on `http://127.0.0.1:1498/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran full `yarn balance-proof` after wiring `--codex` through the final audit. The CLI 30-seed gates still PASSed all target bands: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. Browser 10R gates PASSed 7/7. Browser direct-input gates PASSed 5/5 over 54 Codex-controlled sessions and 26.81 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 1/6 vs 4/6 vs 5/6, intermediate 2/5 legend 0/6 vs 6/6, expert 5/open 0/6 vs 5/6, and master open 0/6. The generated `output/balance-audit.md` now shows `codex-direct-playlog: output/codex-direct-playlog.json (loaded)` and `Codex 직접 조작 보조 증거 분리` PASS with 54 codex-direct sessions / 1608.7 supplemental minutes while human proof remains 0.0/120.0. Representative direct screenshots plus the official web-game client state/screenshot on `http://127.0.0.1:1499/` were inspected; `yarn test`, `yarn build`, and `yarn check` passed. No stat tuning was made because the latest proof still matches the requested balance bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn browser-direct --codex-log=...` Codex supplemental sessions now include a human-readable `minutes` value alongside the existing canonical `seconds`, making the generated `manual-playlog`-compatible JSON easier to inspect and less error-prone for quick summaries. Verified with a short browser-direct runtime smoke on `http://127.0.0.1:1500/` (`--scenarios=noviceHero --max-round=1`) confirming `seconds: 5` and `minutes: 0.08`, a `balance-audit --codex=/tmp/borandi-codex-direct-minutes-smoke.json` smoke showing codex-direct 0.1 minutes while human proof remains missing, screenshot inspection, `yarn test`, `yarn build`, `yarn check`, `manual-playlog --preflight-json`, and the official web-game client/state screenshot on `http://127.0.0.1:1501/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran full `yarn balance-proof` after adding `minutes` to Codex direct sessions, regenerating the ignored local evidence files with the current schema. The 30-seed CLI gates PASSed all target bands again: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. Browser 10R gates PASSed 7/7. Browser direct-input gates PASSed 5/5 over 54 Codex-controlled sessions and 27.40 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 0/6 vs 5/6 vs 6/6, intermediate 2/5 legend 0/6 vs 3/6 with lower pressure, expert 5/open 0/6 vs 5/6, and master open 0/6. `output/codex-direct-playlog.json` now has `minutes` on all 54 sessions (`missingMinutes=0`, total field sum 1644.2), and `output/balance-audit.md` shows `codex-direct 54세션 1644.2분` while human proof remains 0.0/120.0. Representative direct screenshots plus the official web-game client state/screenshot on `http://127.0.0.1:1502/` were inspected; `yarn test`, `yarn build`, `yarn check`, and `manual-playlog --preflight-json` passed. No stat tuning was made because every current automatic/browser/direct gate still matches the requested balance bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Tightened the browser direct-input observation gates so "clear access" rows require actual clear access, not just directional improvement. Novice now uses a clear-access helper, normal requires 1~2-legend clear access plus improvement over no-legend, intermediate requires 2-legend failure plus 5-legend clear access, and expert requires 5-legend failure plus open-budget clear access. `balance-audit` fallback logic mirrors those stricter checks for older direct JSON without observations. Verified with focused audit coverage, a 6-seed intermediate direct-input runtime smoke on `http://127.0.0.1:1503/` showing 2-legend 0/6 vs 5-legend 4/6 clears under the new gate, screenshot inspection, `yarn test`, `yarn build`, `yarn check`, `manual-playlog --preflight-json`, and the official web-game client/state screenshot on `http://127.0.0.1:1504/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran full `yarn balance-proof` after tightening direct-input clear-access gates. The 30-seed CLI gates PASSed all five target bands: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. Browser 10R gates PASSed 7/7. Browser direct-input gates PASSed 5/5 under the stricter clear-access checks over 54 Codex-controlled sessions and 27.66 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 1/6 vs 4/6 vs 6/6, intermediate 2/5 legend 0/6 vs 6/6, expert 5/open 0/6 vs 5/6, and master open 0/6. Representative direct screenshots were inspected for intermediate 5-legend clear, expert open clear, and master early failure. The official web-game client/state screenshot on `http://127.0.0.1:1505/` also passed and still exposes the fixed-map 1~40R contract. No stat tuning was made because every automatic/browser/direct gate matches the requested bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Improved the real human manual-proof start flow: direct `yarn manual-playlog --start ... --notes='<target label>'` now recognizes target labels, prints the same goal-specific finish template as `--start-next`, and rejects a target label whose difficulty does not match the requested `--difficulty`. This makes the direct-start template shown by `--next` safer for real 12+ minute sessions. Verified with focused manual-playlog tests, a direct-start dry-run CLI smoke for the intermediate 5-legend target, `yarn test`, `yarn build`, `yarn check`, `manual-playlog --preflight-json`, and the official web-game client/state screenshot on `http://127.0.0.1:1506/`. No stat tuning was made in this step. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Clarified the in-game and README manual-proof guidance after direct `--start` target labels were made active. The `Tools > 수동 밸런스 증거` modal now tells testers that the direct start marker stores the current target label and that the dry-run finish template should be checked against the target condition; README documents that `manual-playlog` rejects mismatched target-label/difficulty pairs. Verified with focused manual-proof command/manual-playlog tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client/state screenshot on `http://127.0.0.1:1507/`, a live run `render_game_to_text` check confirming `manualProof.commands.start` includes `--notes`, `--startedAt`, and the real seed, plus `manual-playlog --preflight-json`. No stat tuning was made in this step. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Tightened manual proof metadata validation so `yarn manual-playlog` now reads the actual stage ids from `src/data/stages.ts` and rejects direct saves, `--start`, and `--start-next` markers whose `--stage` is not an integer real map id. Existing/manual JSON summaries also mark out-of-range stage sessions invalid. This prevents nonexistent map ids from counting toward the real 120-minute balance proof. Verified with focused manual-playlog stage-range coverage, `yarn test`, `yarn build`, `yarn check`, `manual-playlog --preflight-json`, and the official web-game client/state screenshot on `http://127.0.0.1:1508/`. No stat tuning was made in this step. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-affirmed the corrected stage/map progression model after the latest user clarification: the player chooses a map only when starting a new game, that selected map stays fixed through 1~40R and the final boss, and clearing the 40R boss only unlocks next-map selection permission for a later new game. Added `maxSelectableStageId()` to make `unlockedStage` explicitly mean "maximum selectable map id" rather than current-run map, updated title/topbar/new-game wording to use "맵 선택권", and added regression coverage proving a newly unlocked next map is not forced until the player chooses it. Also mirrored real-stage-id validation in `balance-audit` so impossible manual stage ids cannot satisfy audit rows. Verified with focused settings/engine tests, `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright state/screenshots on `http://127.0.0.1:1509/` confirming fixed-map state plus visible "선택 맵 고정 · 40R 보스까지" and "맵선택권" UI. No stat tuning was made in this step. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Added a current-run pending marker verification step for the remaining real human balance proof. `yarn manual-playlog --pending-id=<id>` now filters pending start markers by exact id and exits nonzero when the marker is not saved; `--json`/`--pending-id-json` expose the same check structurally. The in-game `수동 밸런스 증거` modal and `render_game_to_text.manualProof.commands` now show `pendingId`/`pendingIdJson` commands for the current run so a tester can save the start marker, verify it exists, and only then spend 12+ minutes on the real play session. README documents the new check. Verified with focused command/manual-playlog tests, full `yarn test`, `yarn build`, `yarn check`, `manual-playlog --preflight-json`, the official web-game client, and Playwright DOM/state/screenshot checks on `http://127.0.0.1:1510/`. No stat tuning was made in this step. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Result manual-playlog commands now carry the result proof label in `--notes`: target-satisfying results are tagged with their exact proof row, such as `일반 1~2전설 40R 클리어 증거`, while non-target/filler runs are tagged as time-only. This applies to direct result saves, pending-id finish saves, and finish-latest saves, making later audits and human review less ambiguous. Verified with focused manual-proof result tests, `yarn test`, `yarn build`, `yarn check`, `manual-playlog --preflight-json`, the official web-game client, and Playwright result-modal/state checks on `http://127.0.0.1:1511/`. No stat tuning was made in this step. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --preflight` now links the start-before-play check to the full remaining 2-hour collection plan: text output shows the remaining plan step count, `yarn manual-playlog --plan`, and the first pending target, while `--preflight-json` exposes `planCommandTemplate`, `remainingPlanStepCount`, and a three-step `remainingPlanPreview`. This keeps the next real 12+ minute session connected to the complete six-target plus 120-minute proof plan instead of only showing a single start marker. Verified with focused manual-playlog tests, `manual-playlog --preflight`/`--preflight-json` smokes, full `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright title-screen text/screenshot checks on `http://127.0.0.1:1512/`. No stat tuning was made in this step. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-audit` now includes the manual proof start-before-play workflow directly in the missing "다음 수동 플레이 세션" row: `yarn manual-playlog --preflight`, `yarn manual-playlog --plan`, the dry-run start marker, and the real start marker are all printed together, including custom `--manual`/`--out` paths. README documents the expanded audit guidance. Verified with focused balance-audit tests, a current `balance-audit` smoke showing the new commands, full `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright title-screen text/screenshot checks on `http://127.0.0.1:1513/`. No stat tuning was made in this step because the latest audit still PASSes automatic/browser gates. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --summary`/`--assert` now print `yarn manual-playlog --preflight` and `yarn manual-playlog --plan` before the recommended start-marker commands when manual proof is incomplete. This means `yarn balance-proof --require-complete` stops before automated gates as before, but its failure output now points directly to the start-before-play check and full collection plan instead of only the next start marker. README documents the expanded assert failure guidance. Verified with focused manual-playlog and balance-proof tests, a current failing `balance-proof --require-complete` smoke confirming the new commands appear, full `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright title-screen text/screenshot checks on `http://127.0.0.1:1514/`. No stat tuning was made in this step because the latest audit still PASSes automatic/browser gates. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --summary-json` now includes a `commandTemplates` object with `preflight`, `preflightJson`, `plan`, `planJson`, `summary`, `summaryJson`, `next`, `nextJson`, `startNext`, and `startNextDryRun`. This gives UI or automation one structured place to read the full real-human proof collection command set, including custom `--out` paths, instead of scraping text output. README documents the JSON field. Verified with focused manual-playlog tests, a current `summary-json` smoke showing the command templates, full `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright title-screen text/screenshot checks on `http://127.0.0.1:1515/`. No stat tuning was made in this step because the latest audit still PASSes automatic/browser gates. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn manual-playlog --summary-json` and `--preflight-json` now expose `resultFieldChecklist`, listing the exact result fields a real 12+ minute human session must preserve from the run/result screen: seed, startedAt, endedAt, dataVersion, stateChecksum, result, round, legends, maxGrade, and minutes. The checklist carries the next target's expected result/round/legend/maxGrade values, and text `--preflight` prints the same checklist before a tester starts the session. README documents the field. Verified with focused manual-playlog tests, current `preflight`/`preflight-json` smokes showing the checklist for the novice no-legend target, full `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright title-screen text/screenshot checks on `http://127.0.0.1:1516/`. No stat tuning was made in this step because the latest audit still PASSes automatic/browser gates. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-tightened the map permission rule after the latest clarification: maps never change at stage/round boundaries, the player chooses one map only at new-game start, and next-map permission is granted only for an exact 40R final-boss clear on the current selectable frontier map. `canUnlockNextStage` now requires `round === 40` instead of accepting later anomalous round values, README wording says "정확히 40라운드 최종 보스", and settings tests cover the over-40 non-unlock case. Also kept the in-game manual proof modal result-field checklist visible so real proof sessions preserve seed/start/end/version/checksum/result/round/legend/max-grade/minutes fields. Verified with `yarn test`, `yarn build`, `yarn check`, the official web-game client, Playwright map-permission DOM/state/screenshots on `http://127.0.0.1:1517/`, and Playwright manual-proof checklist screenshots. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran full `yarn balance-proof --port=1518` on the latest fixed-map tree without changing stats. The 30-seed CLI gate still PASSed all target bands: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. Browser 10R gates PASSed 7/7. Browser direct-input gates PASSed 5/5 over 54 Codex-controlled sessions and 27.20 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 1/6 vs 4/6 vs 5/6, intermediate 2/5 legend 0/6 vs 4/6, expert 5/open 0/6 vs 5/6, and master open 0/6. `output/balance-audit.md` still reports automatic/browser PASS and only the real human manual proof rows as MISSING; representative screenshots were inspected for intermediate 5-legend clear access, expert open-budget clear access, and master failure. No stat tuning was made because the latest direct and automatic evidence still matches the requested difficulty bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Added `yarn manual-playlog --sheet`/`--sheet-md`, a Markdown one-page manual balance play sheet that prints the current human-proof status, start-before-play commands, next required session, remaining 120-minute collection plan, result-field checklist, and execution order. `summary-json.commandTemplates` now includes the sheet command, README documents it, and the in-game `Tools > 수동 밸런스 증거` modal shows/copies the sheet command alongside preflight/plan/summary. Verified with focused manual-playlog tests, full `yarn test`, `yarn build`, `yarn check`, CLI sheet and summary-json smokes, the official web-game client, and Playwright DOM/screenshots on `http://127.0.0.1:1519/` confirming the modal exposes `yarn manual-playlog --sheet`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-proof` now writes the current manual proof play sheet to `output/manual-balance-play-sheet.md` by default, with `--manual-sheet=...` override, after regenerating automatic/browser/direct evidence and the audit. This keeps the next real 120-minute human-play checklist on disk beside the evidence artifacts instead of only printing it to stdout. Verified with focused balance-proof/manual-playlog tests, a CLI sheet smoke, full `yarn test`, `yarn build`, and `yarn check`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-proof` now also writes the structured remaining human-play collection plan to `output/manual-balance-play-plan.json` by default, with `--manual-plan=...` override, using `yarn --silent manual-playlog --plan-json`. This gives the next real 120-minute play pass a machine-readable checklist beside the Markdown sheet and audit. Verified with focused balance-proof coverage, a real `manual-playlog --plan-json` smoke, full `yarn test`, `yarn build`, `yarn check`, and the official web-game client on `http://127.0.0.1:1521/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-proof --require-complete` now writes the manual play sheet and structured plan before running the strict `manual-playlog --assert` check, so an incomplete human-proof run still leaves `output/manual-balance-play-sheet.md` and `output/manual-balance-play-plan.json` ready for the next real session instead of only failing in the terminal. Verified with focused balance-proof tests, a failing `--require-complete` smoke that produced both artifacts before exit 1, full `yarn test`, `yarn build`, `yarn check`, and the official web-game client on `http://127.0.0.1:1522/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Re-ran full `yarn balance-proof --port=1523` on the current tree without changing stats. The 30-seed CLI gates PASSed all target bands: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/80.0%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. Browser 10R gates PASSed 7/7. Browser direct-input gates PASSed 5/5 over 54 Codex-controlled sessions and 26.80 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 0/6 vs 3/6 vs 6/6, intermediate 2/5 legend 0/6 vs 4/6, expert 5/open 0/6 vs 4/6, and master open 0/6. Representative screenshots were inspected for intermediate 5-legend clear, expert open-budget clear, and master failure. No stat tuning was made because every automatic/browser/direct gate still matches the requested difficulty bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions; next required session is novice no-legend 40R clear.
- `yarn balance-proof` now writes `output/manual-balance-preflight.json` by default, with `--manual-preflight=...` override, before the manual play sheet and plan. This captures the current human-proof state (`remainingMinutes`, pending markers, next target, and target rows) as a machine-readable artifact alongside the Markdown sheet and plan. The artifact capture allows nonzero manual-proof states so `--require-complete` still reaches the human-readable `manual-playlog --assert` failure output for missing, pending, or invalid manual evidence. Verified with focused balance-proof tests, a failing `--require-complete` smoke that produced preflight/sheet/plan before exit 1, full `yarn test`, `yarn build`, `yarn check`, and the official web-game client on `http://127.0.0.1:1524/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions; next required session is novice no-legend 40R clear.
- Regenerated the default manual proof artifacts with `yarn balance-proof --require-complete --port=1525` after finding `output/manual-balance-preflight.json` could be stale from a previous complete-looking artifact. The refreshed output now correctly reports `logExists=false`, `remainingMinutes=120`, `targetRowsPassed=0/6`, and next target `입문자 무전설 40R 클리어`. Added balance-proof regression coverage that prewrites stale complete-looking preflight/sheet/plan files and verifies the strict missing-manual path overwrites them before failing. Verified with focused balance-proof tests, full `yarn test`, `yarn build`, `yarn check`, and the official web-game client on `http://127.0.0.1:1526/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Ran a targeted 12-seed browser direct-input sample on `http://127.0.0.1:1529/` for the volatile mid/high bands only: `intermediateTwoLegend`, `intermediateFiveLegend`, `expertFiveLegend`, `expertOpen`, and `masterOpen`. The run passed its observation checks over 29.96 simulated hours: intermediate 2-legend 1/12 clears vs 5-legend 11/12 clears, expert 5-legend 0/12 clears vs open-budget 11/12 clears, and master open-budget 1/12 clears with average 15.0R. Representative screenshots were inspected for intermediate 5-legend clear, expert open-budget clear, and master failure. No stat tuning was made because the expanded direct-input evidence still matches the requested difficulty bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Ran a targeted 12-seed browser direct-input sample on `http://127.0.0.1:1530/` for low/normal bands: `noviceHero`, `normalNoLegend`, `normalOneLegend`, and `normalTwoLegend`. The run passed its observation checks over 25.24 simulated hours: novice no-legend 12/12 clears, normal no-legend 2/12 clears, normal 1-legend 5/12 clears, and normal 2-legend 10/12 clears. Representative screenshots were inspected for novice no-legend 40R clear and normal 2-legend 40R clear. No stat tuning was made because the expanded direct-input evidence still matches the requested novice/normal difficulty bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-proof --check-manual-artifacts` now verifies that `output/manual-balance-preflight.json`, `output/manual-balance-play-sheet.md`, and `output/manual-balance-play-plan.json` still match the current `manual-playlog` preflight/plan state, failing on stale or mismatched artifacts before they can be mistaken for proof. Verified with focused pass/fail artifact-check tests, actual artifact-check CLI, full `yarn test`, `yarn build`, `yarn check`, and the official web-game client on `http://127.0.0.1:1531/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- The in-game `Tools > 수동 밸런스 증거` modal and `render_game_to_text.manualProof.commands` now expose current-state finish checkpoint commands while a run is still in progress: exact pending id, current round, current legend count, current checksum, `--finish`, `--finish-latest`, and dry-run variants. This gives a real 12+ minute tester a mid-run recovery/checkpoint command before the result screen, while still warning that final storage must be re-verified with the result screen's actual `endedAt` and `stateChecksum`. Verified with focused manual proof command/result tests, full `yarn test`, `yarn build`, `yarn check`, the official web-game client, and Playwright modal/state screenshots on `http://127.0.0.1:1532/`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Tightened the "초고수" proof gate from "very hard with up to 5%/10% clear tolerance" to strict 0% clear access for both CLI balance and browser direct-input evidence. `yarn balance --json=output/current-balance.json` now passes with master open 0.0% over 30 seeds and average 16.9R, and `yarn browser-direct --url=http://127.0.0.1:1533/ --seeds=12 --strict --scenarios=masterOpen` passes with master open 0/12 clears and average 14.8R. `balance-audit` now rechecks the master scenario clear rate even if an older observation label says pass. README's enemy-limit summary was corrected to the current data values: novice 100, normal 58, intermediate 40, expert 40, master 30. Verified with focused gate/audit tests, full `yarn test`, `yarn build`, `yarn check`, current `balance-audit`, official web-game client, and inspected master failure screenshot. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Added `intermediateOpen` to browser-direct coverage and audit, making direct-input evidence require 10/10 target scenarios plus a separate "중급자 제한 없음 안정권" row. The first 10-scenario direct run exposed weak separation around intermediate 2-legend, so intermediate enemyLimit was lowered 40->38; expert enemyLimit was also lowered 40->38 to preserve non-increasing pressure limits and keep expert at least as strict. Regenerated current balance/browser-balance/browser-direct/Codex logs: CLI 30-seed PASS (novice 100.0%, normal 3.3%/56.7%/86.7%, intermediate 3.3%/73.3%/100.0%, expert 0.0%/90.0%, master 0.0%); browser-balance 7/7 PASS; browser-direct 10/10 target scenarios and 6/6 observations PASS over 29.89 simulated hours (intermediate 2/5/open 0.0%/100.0%/100.0%, expert 5/open 0.0%/83.3%, master 0.0%). Verified with `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, the official web-game client, and screenshot inspection for intermediate 2-legend failure, intermediate open clear, and master failure. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Browser-direct summaries now record `avgClearedLegendOrBetter`/`avgClearedRound`, and the expert direct-input gate now requires expert open-budget clears to show higher cleared-run legend growth than the intermediate 5-legend budget instead of only checking that open budget is better than expert 5-legend. `balance-audit` mirrors this stricter expert-growth recheck and prints both overall average legends and cleared-run average legends, so older passing observation labels cannot hide a too-low expert clear requirement. Latest verification on `http://127.0.0.1:1535/`: browser-direct 10/10 scenarios PASS over 28.50 simulated hours (normal 0/1/2 = 0.0%/66.7%/100.0%, intermediate 2/5/open = 0.0%/83.3%/100.0%, expert 5/open = 0.0%/83.3% with expert open cleared-run average 27.8 legends vs intermediate 5.0, master 0.0%); CLI balance 30-seed PASS (novice 100.0%, normal 3.3%/56.7%/86.7%, intermediate 3.3%/73.3%/100.0%, expert 0.0%/90.0%, master 0.0%); browser-balance 7/7 PASS. Verified with `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, official web-game client, direct Playwright UI screenshot, and screenshot inspection for expert 5-legend failure, expert open clear, and master failure. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Tightened the CLI `balance-audit` expert row so the automatic 30-seed sample also proves expert open-budget clears require higher average legend growth than the intermediate 5-legend budget. The audit now prints automatic sample clear rate, average reached round, and average legend count for intermediate 5-legend, expert 5-legend, and expert open-budget; it fails if expert open-budget average legends are not at least one above the intermediate 5-legend budget and at least 6. Latest audit PASS evidence: intermediate 5-legend 73.3%, 39.7R, 5.0 legends; expert 5-legend 0.0%, 32.1R, 4.6 legends; expert open-budget 90.0%, 37.2R, 24.2 legends. Verified with focused audit regression, `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, official web-game client on `http://127.0.0.1:1536/`, direct Playwright UI screenshot, and screenshot inspection for expert open clear/master failure. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Relaxed the real-human expert weak-proof target from "고수 5전설 이하 40R 실패" to "고수 5전설 이하 실패" across `manual-playlog`, `balance-audit`, result-screen proof classification, and the in-game manual proof guide. This matches the current tuned balance where expert 5-legend often collapses before 40R; any 12+ minute human expert loss with `legends<=5` can now count as the weak-growth proof row, while expert 6+ still requires a 40R clear. Finish templates now use `round=ROUND_REACHED`, `legends=FINAL_LEGENDS`, and `maxGrade=MAX_GRADE` for that weak expert target. Verified with focused manual/audit/core tests using a 33R expert loss, full `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, official web-game client on `http://127.0.0.1:1537/`, direct Playwright title screenshot, and an expert new-run screenshot showing `증거조건 5전설 이하 실패 목표`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Added focused `manual-playlog` regression coverage for the newly relaxed expert weak target after novice/normal/intermediate target rows are filled. `--start-next` now has an explicit test proving it opens `고수 5전설 이하 실패`, stores that exact pending `notes` label, and emits finish templates with `--round=ROUND_REACHED --legends=FINAL_LEGENDS --maxGrade=MAX_GRADE` rather than the old 40R/5-legend template. A CLI smoke with a temporary partial manual log confirmed the same user-facing output. Verified with focused manual-playlog coverage, full `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, official web-game client on `http://127.0.0.1:1538/`, and an expert new-run screenshot showing the in-game `증거조건 5전설 이하 실패 목표`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Added `manual-playlog` regression coverage for mid-plan wrong-difficulty starts: after novice/normal/intermediate target rows are filled, `--start-next --difficulty=master` must fail because the next required session is expert, and no pending marker may be saved. A CLI smoke reproduced the same user-facing error. Verified with focused manual-playlog tests, full `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, the official web-game client on `http://127.0.0.1:1539/`, and an expert new-run screenshot showing `증거조건 5전설 이하 실패 목표`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Corrected the README manual-proof target wording for expert weak evidence so it matches the current CLI/audit rule: `고수 5전설 이하 실패` may be a pre-40R collapse, while only `고수 6전설 이상` requires a 40R clear. Added focused `manual-playlog` regression coverage that locks README against the stale `고수 40R 5전설 이하 실패` wording. Verified with focused manual-playlog tests, full `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, the official web-game client on `http://127.0.0.1:1540/`, and a direct expert new-run screenshot/state showing `증거조건 5전설 이하 실패 목표`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Tightened the in-game `Tools > 수동 밸런스 증거` guidance so current-run `--start-next` commands no longer read like they can be saved immediately. The modal now tells testers to run the current seed's `--dry-run` first and save the start marker only when that check PASSes, which protects against wasting a 12+ minute session when the current run difficulty is not the next required proof difficulty. Added source-level regression coverage for the modal wording. Verified with focused command/manual-playlog tests, full `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, the official web-game client on `http://127.0.0.1:1541/`, and a modal screenshot/DOM check showing the new dry-run warning. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Improved `yarn manual-playlog --start-next` wrong-difficulty failures so they now print the next required proof session, the correct dry-run command, the correct start-marker command, and a reminder to start the correct difficulty run before replacing `GAME_SEED_HERE`. This keeps a rejected current-run command actionable instead of only showing why it failed. Verified with focused manual-playlog tests, an expected-failure CLI smoke, full `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, the official web-game client on `http://127.0.0.1:1542/`, and a novice new-run screenshot/state showing the next manual target plus dry-run command exposure. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Expanded `yarn manual-playlog --next` and `--next-json` so the one-step next-session view now includes the same execution order and result-field checklist that `--preflight` exposes. A tester who starts from `--next` can now see the required seed, startedAt, endedAt, dataVersion, stateChecksum, result, round, legends, maxGrade, and 12+ minute fields before committing to a real session. Verified with focused manual-playlog tests, `--next`/`--next-json` smokes, full `yarn test`, `yarn build`, `yarn check`, latest `balance-audit`, the official web-game client on `http://127.0.0.1:1543/`, and a novice new-run screenshot/state showing the next manual target plus dry-run command exposure. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- Updated the `balance-audit` missing manual-proof row to point directly to `yarn manual-playlog --next` and `yarn --silent manual-playlog --next-json`, in addition to preflight, plan, and start marker commands. Since `--next` now carries the result-field checklist, the audit report itself now routes a tester to the one-step view needed before a real 12+ minute session. Verified with focused balance-audit tests, current `balance-audit`, full `yarn test`, `yarn build`, `yarn check`, the official web-game client on `http://127.0.0.1:1544/`, and a novice new-run screenshot/state showing the manual target plus dry-run command exposure. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions.
- `yarn balance-proof` now writes the next-session manual proof artifacts beside the existing preflight/sheet/plan files: `output/manual-balance-next.txt` and `output/manual-balance-next.json`, with `--manual-next` and `--manual-next-json` overrides. `--check-manual-artifacts` now validates those next-session files against live `manual-playlog --next-json` plus the human-readable next target text, so stale next-session guidance cannot be mistaken for current proof. Verified with focused balance-proof tests, a temp-path `--require-complete`/`--check-manual-artifacts` smoke, full `yarn test`, `yarn build`, `yarn check`, current `balance-audit`, the official web-game client on `http://127.0.0.1:1546/`, and inspected modal/game screenshots. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions; next required session is novice no-legend 40R clear.
- Pending manual-playlog start markers now expose the exact 12-minute proof-ready timestamp and remaining seconds in `--pending-json`, `--preflight-json`, `--summary-json`, and `--next-json`, and the text `--pending`/`--summary`/`--preflight`/`--next`/`--sheet` outputs print the same `12분 기준 시각`. This makes it clearer when a real human session has crossed the minimum proof duration before finishing the run. Verified with focused manual-playlog tests, a temp pending-marker CLI smoke, full `yarn test`, `yarn build`, `yarn check`, current `balance-audit`, the official web-game client on `http://127.0.0.1:1547/`, and inspected gameplay screenshot/state. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions; next required session is novice no-legend 40R clear.
- The live game now shows the current run's 12-minute manual proof-ready clock in the bottom status label (`수동시간 ... · 12분기준 ...`), and `render_game_to_text.manualProof.targetReadyAt` exposes the same ISO timestamp for browser checks and result capture tooling. This aligns the in-game timer with the CLI pending-marker ready time so a real tester can see when a run becomes eligible without leaving the game. Verified with focused manualProof helper tests, full `yarn test`, `yarn build`, `yarn check`, current `balance-audit`, the official web-game client on `http://127.0.0.1:1548/`, and inspected gameplay screenshot/state. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes and 0/6 target sessions; next required session is novice no-legend 40R clear.
- Re-locked the stage-system wording around the latest clarification: game logs now say a run starts on an "이번 판 고정 맵", the result log says a 40R final-boss clear only opens next-map selection in a later new game, and the new-game modal now says "이번 판 고정 맵 선택" with explicit 1~40R fixed-map copy. The engine regression test now records stage ids after the 10/20/30/40 boss checkpoints and proves every boss clear stays on the selected map. The Poly TD reference doc now calls out that its original next-stage flow is not this project's map progression model. Verified with focused engine/settings tests, full `yarn test`, `yarn build`, `yarn check`, the official web-game client on `http://127.0.0.1:1550/`, Playwright state checks showing `fixedForRun=true`, `changesBetweenRounds=false`, and `nextMapPermissionOnly=true`, plus inspected new-run modal and gameplay screenshots.
- `yarn balance-proof --check-manual-artifacts` now tells the tester to run `yarn balance-proof --require-complete` when any manual proof guidance artifact is missing, instead of only listing the missing files. The current ignored output artifacts are present and `--check-manual-artifacts` passes against the live empty human log state. Verified with focused balance-proof tests, full `yarn test`, `yarn build`, `yarn check`, current `balance-audit`, the official web-game client on `http://127.0.0.1:1551/`, Playwright state checks showing the next manual target and 12-minute proof clock, plus inspected modal/game screenshots. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- `render_game_to_text.manualProof.commands` now includes the full manual proof status command set (`preflight`, `preflightJson`, `next`, `nextJson`, `summary`, `summaryJson`, `plan`, `planJson`, `sheet`) in addition to current-run start/finish commands, and the in-game manual proof modal now also prints `yarn --silent manual-playlog --next-json`. This lets a tester or browser-side check compare the live run with the CLI's next required 2-hour proof step without scraping README text. Verified with focused command-helper tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, current `balance-audit`, the official web-game client on `http://127.0.0.1:1552/`, Playwright state checks confirming the new command keys, and inspected the manual proof modal screenshot. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Ended-run `render_game_to_text.manualProof` now includes structured result proof fields: `resultTarget`, `resultChecks`, and `resultPassed`. This makes the result-screen proof verdict machine-readable for the remaining 2-hour direct-play collection instead of requiring DOM/text scraping. Verified with focused proof tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, current `balance-audit`, the official web-game client on `http://127.0.0.1:1553/`, and a DEV-aged master failure smoke showing `resultTarget="초고수 실패 기록 증거"`, `resultPassed=true`, and result checks for 13.0 minutes plus loss. That DEV-aged smoke was only a structure/runtime check and is not counted as real human proof. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- `yarn --silent manual-playlog --summary-json` now exposes structured proof progress as `progress.totalMinutes`, `progress.perDifficultyMinutes`, and `progress.targetRows`, each with `current`, `required`, `remaining`, `ratio`, and `percent`. The empty real-human proof state currently reports 0/120 minutes, 0/12 minutes for every difficulty, 0/6 target rows, and next required session `입문자 무전설 40R 클리어`. Also hardened numeric DEV seeds by normalizing `ctx.newRun` seeds to strings and making `shellArg` stringify runtime values, after a browser smoke exposed `render_game_to_text` failing on `__randi_dev.newRun(1554, ...)`. Verified with focused command/manual-playlog tests, full `yarn test`, `yarn build`, `yarn check`, the official web-game client on `http://127.0.0.1:1554/`, Playwright state/screenshot inspection confirming `summaryJson` is exposed and seed `"1554"` is stable, `yarn balance-proof --check-manual-artifacts`, current `balance-audit`, and `manual-playlog --summary-json` progress output. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Re-ran the latest 6-seed browser direct-input sampler on `http://127.0.0.1:1555/` without changing stats. `yarn browser-direct --url=http://127.0.0.1:1555/ --seeds=6 --strict --json=output/browser-direct-latest-20260621-1555.json --screenshots=output/browser-direct-latest-20260621-1555-shots --codex-log=output/codex-direct-playlog-latest-20260621-1555.json` passed all 6 observation gates over 60 Codex-controlled sessions and 28.99 simulated hours: novice no-legend 6/6 clears; normal 0/1/2 legend 1/6 vs 4/6 vs 5/6 clears; intermediate 2/5/open 0/6 vs 6/6 vs 6/6 clears; expert 5/open 0/6 vs 5/6 clears with open-budget cleared-run average 27.6 legends; master open 0/6 clears. Combined audit with the new direct JSON reports automatic/browser PASS and only real human manual proof rows as MISSING. Screenshots were inspected for normal 2-legend clear, intermediate 2-legend failure, intermediate 5-legend clear, expert 5-legend failure, expert open clear, and master early failure. No stat tuning was made because the latest direct-input evidence still matches the requested difficulty bands. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- `yarn browser-direct` JSON now records real wall-clock timing separately from simulated play time: top-level `startedAt`, `endedAt`, `wallClockSeconds`, `totalWallClockSeconds`, plus per-run/per-scenario wall-clock fields. `balance-audit` prints wall-clock minutes when present while preserving compatibility with older direct JSON that only has simulated seconds. README now documents that browser-direct's long play hours are simulated helper evidence, not a replacement for human-playtest time. Verified with focused `balance-audit` regression coverage, a short browser-direct smoke on `http://127.0.0.1:1556/` confirming `wallClockSeconds: 1.21`, `totalSimulatedSeconds: 5`, and audit text `0.02 wall-clock minutes`, screenshot inspection, full `yarn test`, `yarn build`, `yarn check`, and the latest 6-seed audit remaining automatic/browser PASS while real human proof stays MISSING. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Manual proof result summaries now record successful play input evidence as `inputCount` plus `inputCounts`, and generated `manual-playlog` result/finish commands include `--inputCount=...`. `yarn manual-playlog` and `balance-audit` now require human-playtest result rows to carry `inputCount >= 1`, while existing `codex-direct` helper logs remain accepted as auxiliary evidence. README, example manual JSON, result-field checklists, result reports, `render_game_to_text.manualProof.evidenceFields`, and browser-direct Codex logs were updated to expose the same field. Verified with focused/manual/audit/proof tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, latest `balance-audit`, the official web-game client on `http://127.0.0.1:1557/`, and a Playwright gameplay screenshot/state check confirming `inputCount=4` appears in both `manualProof.evidenceFields` and the current finish command. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Tightened the human-playtest input proof threshold from merely present to at least 12 successful play inputs per logged result. `yarn manual-playlog` now rejects human result saves below 12 inputs, `balance-audit` only counts human sessions with `inputCount >= 12`, and the result-field checklist/UI/README/default manual artifacts now say `inputCount` must be `12 이상`. Codex-direct auxiliary logs remain separate and are not counted as human proof. Verified with focused manual/audit/proof tests, full `yarn test`, `yarn build`, `yarn check`, refreshed `yarn balance-proof --check-manual-artifacts`, current `balance-audit`, the official web-game client on `http://127.0.0.1:1558/`, and Playwright modal screenshots showing the `inputCount` row expects `12 이상`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Manual proof result commands and logs now also preserve input type evidence. `render_game_to_text` exposes `inputCounts`, generated current/result/finish commands include `--inputTypes=...`, `yarn manual-playlog` rejects human result saves without at least one input type, `balance-audit` only counts human sessions that have both `inputCount >= 12` and input types, and browser-direct Codex auxiliary logs carry the same field for separation. README, example manual JSON, result-field checklists, and default manual artifacts now list `inputTypes` as a required result field. Verified with focused manual/audit/proof tests, full `yarn test`, `yarn build`, `yarn check`, refreshed `yarn balance-proof --check-manual-artifacts`, current `balance-audit`, the official web-game client on `http://127.0.0.1:1559/`, and Playwright modal/state checks confirming `inputCounts={summon:3,startWave:1}` plus `--inputTypes='startWave,summon'`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Manual proof result commands and logs now preserve input-by-type counts as first-class evidence. Generated direct/finish/finish-latest commands include `--inputCounts=type:count,...`, `yarn manual-playlog` rejects human-playtest rows without input counts or with a total that does not match `inputCount`, and `balance-audit` applies the same check before counting a human session. README, example manual JSON, browser-direct auxiliary logs, result-field checklists, and default manual artifacts now list `inputCounts` as required with `합계가 inputCount와 일치`. Rechecked the clarified map-permission model at the same time: engine/settings tests still prove a selected map stays fixed through the 10/20/30/40 boss checkpoints and only the 40R final-boss clear grants later new-game selection permission; browser validation on `http://127.0.0.1:1560/` confirmed unlockedStage=2 enables stages 1~2, keeps stage 3 locked, and `render_game_to_text.stage` reports `fixedForRun=true`, `autoChangesAfterRoundOrBoss=false`, and `unlockAddsSelectionPermissionOnly=true`. Verified with focused stage/manual tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, latest `balance-audit`, the official web-game client, and inspected `output/web-game/stage-permission-1560.png`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Result-screen manual proof capture now has a JSON path: `증거 JSON 내보내기` writes a `manual-playlog-result` JSON containing the result summary and proof note, and `yarn manual-playlog --from-result=FILE.json` imports that JSON into the same validation/save path used by the long CLI command. This reduces copy errors for `seed`, `startedAt`, `endedAt`, `dataVersion`, `stateChecksum`, `inputCount`, `inputTypes`, and `inputCounts` while preserving all existing human-playtest checks. README documents the `--from-result` dry-run-first workflow. Verified with a new manual-playlog import regression, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, latest `balance-audit`, the official web-game client on `http://127.0.0.1:1561/`, and inspected result-modal screenshots `output/web-game/manual-result-json-1561.png` and `output/web-game/manual-result-json-bottom-1561.png`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Manual proof guidance now routes testers through the result JSON path everywhere the next 12+ minute session is described. `yarn manual-playlog --next`, `--preflight`, `--sheet`, `--summary-json`, `--preflight-json`, and `balance-audit` now include `yarn manual-playlog --from-result=PATH_TO_EXPORTED_JSON --dry-run` and the matching save command, so a real tester can finish a session from the exported result JSON without manually copying every result field. The workflow text now explicitly says to export the result JSON, dry-run validate it, then remove `--dry-run` to store the proof row. Verified with focused manual/audit tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, current `manual-playlog --next`/`--sheet`/`balance-audit` output checks, the official web-game client on `http://127.0.0.1:1562/`, and inspected `output/web-game/manual-next-flow-1562.png`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Result JSON capture now also supports copy/paste verification: the result modal has `증거 JSON 복사`, and `yarn manual-playlog --from-result=-` / `--from-result-stdin` reads a copied `manual-playlog-result` JSON from standard input, so macOS testers can run `pbpaste | yarn manual-playlog --from-result=- --dry-run` before saving. README and the next-session guidance expose the stdin command templates. Verified with focused manual/result tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, current `manual-playlog --next`/`--summary-json` output checks, the official web-game client on `http://127.0.0.1:1563/`, and a browser clipboard check plus screenshot `output/web-game/manual-result-json-copy-1563.png`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- `balance-audit` now repeats the copied-result JSON stdin workflow in the missing "다음 수동 플레이 세션" row: `pbpaste | yarn manual-playlog --from-result=- --dry-run` for validation and the same command without `--dry-run` for saving. README's `--next-json`/`--plan-json` description now names the `fromResultStdinDryRun`/`fromResultStdin` templates too, so the audit and JSON guidance agree. Verified with focused audit/manual tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, live `balance-audit` output grep, the official web-game client on `http://127.0.0.1:1564/`, and inspected `output/web-game/audit-stdin-guide-1564.png`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- `manual-playlog --next`, `--preflight`, `--sheet`, `--next-json`, and `--preflight-json` now carry the copied-result JSON flow all the way through actual saving, not just dry-run validation. The visible execution order says testers can export or copy the result JSON, validate with either `--from-result=PATH... --dry-run` or `--from-result=- --dry-run`, then remove `--dry-run` from the same JSON command to save; the sheet now includes a separate `표준입력 저장` section. Regression coverage now verifies stdin result JSON can be both dry-run checked and saved via `--from-result-stdin`. Verified with focused manual-playlog tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, live `manual-playlog --next`/`--next-json`/`--sheet` output checks, the official web-game client on `http://127.0.0.1:1565/`, and inspected `output/web-game/stdin-save-guide-1565.png`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- `manual-playlog` now supports `--from-clipboard`, which reads the copied result JSON directly through macOS `pbpaste`, so after pressing `증거 JSON 복사` a tester can run `yarn manual-playlog --from-clipboard --dry-run` and then the same command without `--dry-run` instead of piping through `pbpaste`. `--next`, `--preflight`, `--sheet`, `--next-json`, `--preflight-json`, `--summary-json`, README, and `balance-audit` now expose the clipboard validation/save commands alongside file and stdin import. Regression coverage uses a fake `pbpaste` binary to prove clipboard JSON can be dry-run checked and saved. Verified with focused manual/audit tests, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, live `manual-playlog --next`/`--next-json`/`balance-audit` output checks, the official web-game client on `http://127.0.0.1:1566/`, and inspected `output/web-game/clipboard-proof-guide-1566.png`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- The result modal now shows the `--from-clipboard` validation/save commands directly next to the result JSON export flow, and the `증거 JSON 복사` toast tells the tester to run `--from-clipboard --dry-run` while the JSON remains on the clipboard. This avoids adding a command-copy button that would overwrite the copied JSON. Verified with focused modal/source coverage, full `yarn test`, `yarn build`, `yarn check`, `yarn balance-proof --check-manual-artifacts`, the official web-game client on `http://127.0.0.1:1567/`, and a result-modal clipboard smoke that copied JSON, parsed it from the clipboard, checked `inputCount=12`, and inspected `output/web-game/result-clipboard-command-1567.png`. Automatic/browser evidence still passes, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Re-locked the user's intended map-permission stage model at the engine level: the map selected at new-game start is now stored as the run's fixed map and restored before ticks, round starts, round completion, game end, and result summaries. Even if a caller mutates `state.stageId` mid-run, the engine returns to the selected start map, so maps cannot change between rounds or boss clears. Added a regression test for that bad mutation path. Verified with focused engine/settings tests, full `yarn test`, `yarn build`, `yarn check`, the official web-game client on `http://127.0.0.1:1569/`, and a Playwright state/screenshot check at `output/web-game/fixed-map-contract-1569.png` confirming `fixedForRun=true`, `autoChangesAfterRoundOrBoss=false`, and `nextMapPermissionOnly=true`.
- Result-screen proof capture now has one-paste embedded JSON commands: `JSON포함 검증 복사` and `JSON포함 저장 복사` copy a heredoc command containing the current `manual-playlog-result` JSON, so a tester can paste a single terminal command without relying on the JSON remaining in the clipboard. Modal button rows now wrap cleanly while keeping button labels horizontal. Verified with focused manual-playlog modal coverage, full `yarn test`, `yarn build`, `yarn check`, the official web-game client on `http://127.0.0.1:1570/`, and Playwright clipboard/layout checks plus inspected screenshot `output/web-game/result-heredoc-command-1570-after-css.png`. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Added a one-paste `JSON포함 저장+다음 복사` result button that embeds the result JSON, saves it through `yarn manual-playlog --from-result=-`, and only then runs `yarn manual-playlog --next` on success. This makes the real 2-hour proof loop shorter after each finished session. Verified with focused modal coverage, full `yarn test`, `yarn build`, `yarn check`, the official web-game client on `http://127.0.0.1:1571/`, and Playwright clipboard/layout checks plus inspected screenshot `output/web-game/result-heredoc-save-next-1571.png`. Current authoritative manual summary still reports no `output/manual-balance-playlog.json`: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Added a safer one-paste `JSON검증+저장+다음 복사` result button. It writes the embedded result JSON to a temp file, runs `yarn manual-playlog --from-result="$tmpfile" --dry-run`, then saves the same file and runs `yarn manual-playlog --next` only if validation/save succeed, cleaning the temp file afterward. Verified with focused modal coverage, full `yarn test`, `yarn build`, `yarn check`, the official web-game client on `http://127.0.0.1:1572/`, and Playwright clipboard/layout checks plus inspected screenshot `output/web-game/result-heredoc-validate-save-next-1572.png`. Current authoritative manual summary still reports no `output/manual-balance-playlog.json`: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Re-ran full `yarn balance-proof --port=1573` on the latest tree after the result-proof workflow changes. The 30-seed CLI gate PASSed all five bands: novice no-legend 100.0%, normal 0/1/2 legend 3.3%/56.7%/86.7%, intermediate 2/5/open 3.3%/73.3%/100.0%, expert 5/open 0.0%/90.0%, and master open 0.0%. Browser 10R gates PASSed 7/7. Browser direct-input gates PASSed 6/6 observations over 60 Codex-controlled sessions and 30.27 simulated hours: novice no-legend 6/6 clears, normal 0/1/2 legend 0/6 vs 3/6 vs 6/6, intermediate 2/5/open 0/6 vs 5/6 vs 6/6, expert 5/open 0/6 vs 5/6, and master open 0/6. Representative screenshots were inspected for novice no-legend 40R clear, normal no-legend failure, intermediate 5-legend 40R clear, expert 5-legend failure, master early failure, and browser 10R normal 2-legend success. No stat tuning was made because every automatic/browser/direct gate still matches the requested difficulty bands and the fixed-map 1~40R contract remains visible in the UI. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Added `--real-time` to `yarn browser-direct` so Codex-controlled direct play can optionally wait on real wall-clock time instead of fast-forwarding with `advanceTime`. The generated JSON now records `timingMode`, `realTime`, `totalPlaySeconds`, and per-session `wallClockBased` while still marking these logs as `codex-direct-playtest`, so they remain separated from `human-playtest` 120-minute proof. Verified with `node --check scripts/browser-direct-playtest.mjs`, full `yarn test`, `yarn build`, `yarn check`, a real-time browser smoke on `http://127.0.0.1:1574/` (`--real-time --seeds=1 --scenarios=noviceHero --max-round=1 --step-ms=1000`) confirming `totalPlaySeconds=10.407`, `timingMode=real-time`, and `wallClockBased=true`, a default simulated smoke confirming the original fast path still records `timingMode=simulated` behavior, inspected `output/browser-direct-realtime-smoke-shots/noviceHero.png`, and ran the official web-game client with state output confirming the fixed-map contract (`fixedForRun=true`, `autoChangesAfterRoundOrBoss=false`, `nextMapPermissionOnly=true`). This improves future Codex wall-clock play evidence, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required session `입문자 무전설 40R 클리어`.
- Ran the first full real-time Codex direct-play evidence session with `yarn browser-direct --url=http://127.0.0.1:1575/ --real-time --seeds=1 --scenarios=noviceHero --max-round=40 --step-ms=5000 --json=output/browser-direct-realtime-novice-20260621.json --screenshots=output/browser-direct-realtime-novice-20260621-shots --codex-log=output/codex-direct-realtime-novice-20260621.json`. The run waited on wall-clock time, reached 40/40R, cleared with 0 legend-or-better units, 0/100 pressure, 351 successful play inputs, and recorded 22.38 real play minutes (`timingMode=real-time`, `wallClockBased=true`). `yarn balance-audit --codex=output/codex-direct-realtime-novice-20260621.json --out=output/balance-audit-realtime-novice-20260621.md` reports `Codex 직접 조작 보조 증거 분리` PASS with 1 codex-direct session / 22.4 minutes while human remains 0.0 minutes. Inspected `output/browser-direct-realtime-novice-20260621-shots/noviceHero.png`, which shows 40R clear, `무전설 유지`, `수동증거 12:00+ 충족`, and the selected map fixed through the final boss. This is stronger wall-clock Codex direct-play evidence for the novice band, but real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required human session `입문자 무전설 40R 클리어`.
- Ran a full real-time Codex direct-play sample for the normal 2-legend condition with `yarn browser-direct --url=http://127.0.0.1:1576/ --real-time --seeds=1 --scenarios=normalTwoLegend --max-round=40 --step-ms=5000 --json=output/browser-direct-realtime-normal-two-20260621.json --screenshots=output/browser-direct-realtime-normal-two-20260621-shots --codex-log=output/codex-direct-realtime-normal-two-20260621.json`. This single run waited 36.47 real minutes and ended at 39/40R with 2 legend-or-better units, pressure 64/64, 253 successful play inputs, and result loss. The screenshot `output/browser-direct-realtime-normal-two-20260621-shots/normalTwoLegend.png` was inspected and shows 2-legend 유지, 12:00+ proof time, fixed map, and 39R failure. `yarn balance-audit --codex=output/codex-direct-realtime-normal-two-20260621.json --out=output/balance-audit-realtime-normal-two-20260621.md` reports `Codex 직접 조작 보조 증거 분리` PASS with 1 codex-direct session / 36.5 minutes while human remains 0.0 minutes. Treat this as a noisy single real-time failure sample: it does not overturn the current 30-seed CLI and 6-seed browser-direct gates, which still show normal 1~2 legend conditions are much better than no-legend and in the intended clear-access band. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required human session `입문자 무전설 40R 클리어`.
- Tuned normal difficulty slightly after the real-time 2-legend sample failed exactly at the pressure cap: `normal.enemyLimit` is now 60 instead of 58. This is intentionally narrow and leaves HP/economy/unit caps unchanged. Verification after the tweak: `yarn test`, `yarn build`, and `yarn check` passed; `yarn balance --seeds=30 --json=output/current-balance.json` PASSed all five gates with normal 0/1/2 legend at 3.3%/56.7%/86.7%; `yarn browser-balance --url=http://127.0.0.1:1578/ --json=output/browser-balance.json --screenshots=output/browser-balance-shots` PASSed 7/7, including normal 2-legend reaching 11R at pressure 0/66 while normal 2-hero failed at 7R; full `yarn browser-direct --url=http://127.0.0.1:1578/ --seeds=6 --strict --json=output/browser-direct.json --screenshots=output/browser-direct-shots --codex-log=output/codex-direct-playlog.json` PASSed all observations over 60 sessions / 28.77 simulated hours. The updated browser-direct normal band is now 0-legend 1/6 clears, 1-legend 1/6 clears, and 2-legend 6/6 clears, making the 2-legend clear-access band much less razor-thin while still keeping no-legend weak. `yarn balance-audit --balance=output/current-balance.json --browser=output/browser-balance.json --direct=output/browser-direct.json --codex=output/codex-direct-playlog.json --out=output/balance-audit.md` reports automatic/browser PASS and only human manual proof rows as MISSING. Inspected updated screenshots for normal no-legend failure, normal 2-legend 40R clear, intermediate 2-legend failure, and master early failure. Real `output/manual-balance-playlog.json` evidence remains absent: human 0.0/120.0 minutes, 0/6 target sessions, next required human session `입문자 무전설 40R 클리어`.
- Rechecked the stage/map progression contract after the latest user correction. The current implementation already matches the requested model: the map is chosen only when starting a new game, the selected map is fixed through 1~40R and all boss rounds, and clearing the 40R final boss only adds the next map to the selectable permission range for a later new game. No code change was needed; focused engine/settings tests passed, and `yarn build` plus `yarn check` passed. The current result screen still offers only explicit "새 게임" map choice rather than auto-starting or preselecting the newly unlocked next map.
- Re-ran the normal 2-legend real-time Codex direct sample after the `normal.enemyLimit = 60` tuning: `yarn browser-direct --url=http://127.0.0.1:1579/ --real-time --seeds=1 --scenarios=normalTwoLegend --max-round=40 --step-ms=5000 --json=output/browser-direct-realtime-normal-two-after-limit60-20260621.json --screenshots=output/browser-direct-realtime-normal-two-after-limit60-20260621-shots --codex-log=output/codex-direct-realtime-normal-two-after-limit60-20260621.json` waited 34.28 real minutes and cleared 40/40R with exactly 2 legend-or-better units, pressure 36/66, and 244 successful play inputs. Inspected `output/browser-direct-realtime-normal-two-after-limit60-20260621-shots/normalTwoLegend.png`, which shows the fixed selected map, 40/40 clear, `2전설 유지`, and `수동증거 12:00+ 충족`. `yarn balance-audit --codex=output/codex-direct-realtime-normal-two-after-limit60-20260621.json --out=output/balance-audit-realtime-normal-two-after-limit60-20260621.md` reports the Codex direct auxiliary row PASS with 1 session / 34.3 minutes, while human manual proof remains 0.0/120.0 minutes and 0/6 target sessions.

## TODO

- The requested "about 2 hours of direct play" balance pass is still not completed; current evidence is automated autoplay plus short browser UI/playtest smoke checks.
